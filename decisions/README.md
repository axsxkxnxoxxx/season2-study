# Decision log

Tracked, public, aggregates and rationale only — no usernames, user IDs, or individual watch
histories. One file per decision, numbered in the order decided.

This folder is the decision log of record. Agent memory is not the decision log: it is an
agent's working continuity and is not the artifact. Where a decision file and an agent's memory
differ, this folder governs; where a decision file and the deliverable it approves differ, the
deliverable governs on the substance and this folder governs on who decided what, and when.

Step 18 assembles the final log from these files.

| # | Decision | Decided | Status |
| :--- | :--- | :--- | :--- |
| [0001](0001-step1-outcome-definition-gate.md) | **Step 1 outcome definition APPROVED** (gate 1 of 5). `H = 91 days` and the D12 cadence thresholds adopted by name; `pull_date` adopted in form with its value deferred; Red Team B2 overruled as accepted risk. | 2026-08-10 | Closed |
| [0002](0002-step4-history-endpoint.md) | **Step 4 source is `GET /users/:id/history`, unfiltered, one sweep per user.** Replaces `/users/:id/watched/shows`, which returns no per-episode timestamps. | 2026-08-10 | Closed |
| [0003](0003-w-estimation-sample.md) | **W is estimated on bucket C1 (all-at-once) shows only**, per D12, and applied to all shows. Closes Step 1 open question 2. | 2026-08-10 | Closed |
| [0004](0004-403-handling.md) | **A 403 on a user resource skips that user and continues**, bounded by two circuit breakers; any other 403 still hard stops; ambiguity resolves strict. Amends `CLAUDE.md` API discipline. | 2026-08-10 | Closed |
| [0005](0005-step3-stopping-rule.md) | **Step 3 stopped on `TARGET_USABLE = 4000`, not on the plateau rule `task-sheet.md` names.** The plateau rule ran 36 rounds and never fired. Agent-taken; departs from the task sheet. | 2026-08-11 | **Open — needs ratification** |
| [0006](0006-step3-crawl-constants.md) | **The twelve Step 3 crawl constants**, none of which appear in `task-sheet.md`. Two carry known consequences: the usable floor and a FIFO screening-order artifact. Agent-taken. | 2026-08-11 | **Open — needs ratification** |
| [0007](0007-step3-channel-cost-trade.md) | **Channel A took 89 percent of discovery calls at 5× the cost per user** on whole-run averages — though it was the *cheaper* channel over the last third. Defensible as buying Step 11 arm independence. Agent-taken. | 2026-08-11 | **Open — needs ratification** |
| [0008](0008-step3-seed-source.md) | **Step 3 seeded from movie-comment authors.** Satisfies the task sheet's prohibition but biases the pool toward heavy trackers — **downward on the headline**, compounding with the liveness bias. The highest-consequence agent choice in Step 3. | 2026-08-11 | **Open — needs ratification** |
| [0009](0009-step4-pull-order.md) | **Step 4 pulls in stratified round-robin order** over ten equal-count forecast-page bins, so any early stop leaves a prefix proportional across the full distribution. Amends an initial median-out instruction, which left a *centered* slice with no heavy users. | 2026-08-11 | Closed |
| [0010](0010-step4-tail-cap.md) | **Tail cap at 300 forecast pages**, skip whole and never truncate, with an actual-pages guard that discards mid-sweep overruns. Justified as a **circuit breaker on forecast error**, not as protection against a slow user. Excludes 0.93% of the pool; direction **upward** on the headline. | 2026-08-11 | Closed |
| [0011](0011-pull-date-value.md) | **`pull_date = 2026-08-11`**, `τ_pull = 2026-08-11T00:00:00Z`. Closes the value D11 deferred until Step 4's schedule was known. Constraint satisfied: earliest per-user fetch is 05:01:26Z. | 2026-08-11 | Closed |
| [0012](0012-sweep-completeness-rule.md) | **Sweep completeness is full `page_count` coverage plus a 2% residual tolerance**, not exact match on `item_count` — which the pilot showed is not an exact record count. Over-count, under-count and genuine cross-page duplicates are counted **separately**. **Amends [0002](0002-step4-history-endpoint.md) and an approved gate artifact.** | 2026-08-11 | Closed |
| [0013](0013-step2-execution-delegation.md) | **Step 2 execution is delegated to an agent; the selection rules stay with the Human Lead.** Judgment is which shows belong and on what criterion; execution is fetching seasons and applying a written rule. **Amends the `CLAUDE.md` Human Lead ownership rule, for Step 2 only.** Three conditions: no concurrent live-API agents, candidate set recomputed on the full pool, underspecified rules reported not resolved. | 2026-08-11 | Closed |
| [0014](0014-no-content-filters-structural-fields.md) | **No content-category filters in the Step 2 frame.** The anime and daily-strip/soap exclusions are dropped before first use — the concern was release structure, not genre, and genre is a lossy proxy for it in both directions. Release structure is recorded as **fields**; thresholds set by the Human Lead after the distributions are visible. **Both open items now discharged: platform fragmentation by [0016](0016-per-season-network-dropped.md), the thresholds by [0020](0020-step2-structural-thresholds.md).** | 2026-08-11 | **Closed 2026-08-12** |
| [0015](0015-step2-unaired-s2-exclusion.md) | **A listed-but-unaired season 2 is not a season 2 for the frame.** 12 shows excluded, all reporting `aired_episodes = 0`. Recorded as its own ledger step rather than folded into the date cutoff, which removed 60 — collapsing them would have made the ledger say 72. Resolves the one case the selection rules did not decide. | 2026-08-12 | Closed |
| [0016](0016-per-season-network-dropped.md) | **Per-season network dropped as a field; platform fragmentation is not a variable in this study.** 0.71% populated across 6,645 season objects; one show in 2,094 carries two distinct values, read as noise and not as fragmentation. **Closes the first open problem in [0014](0014-no-content-filters-structural-fields.md); the second survives and now attaches to the show-level network**, a present-day value that must not be read as release-time availability. | 2026-08-12 | Closed |
| [0017](0017-air-period-definition.md) | **Air period := calendar year of the S2 finale, bucketed pre-2020 / 2020–2022 / 2023–2025**, bracketing the production shutdown and claiming nothing finer. Carries a confound that travels with it: **air period and cadence are strongly collinear** on this frame and are not independent cuts. | 2026-08-12 | Closed |
| [0018](0018-size-quintile-base.md) | **The title size quintile is cut over the 1,226-show frame, not the 2,094 candidates** — the quintile cuts results, and results exist only in-frame. The rejected base gave unequal bins labelled as quintiles. A quintile label is **not a stable identifier**: rebuild the frame and every boundary moves. | 2026-08-12 | Closed |
| [0019](0019-pool-completers-recomputed.md) | **`pool_completers` recomputed on real season lengths; the max-observed proxy is superseded and no result may use it.** Changes nothing on this frame — proxy `L1_hat` equals real `\|E1\|` on 1,225 of 1,226 shows — which is not a rehabilitation of the proxy generally. **Corrects the stated premise of [0013](0013-step2-execution-delegation.md) condition 2:** counts do not only rise; 118 long-tail shows fell between pool sizes. | 2026-08-12 | Closed |
| [0020](0020-step2-structural-thresholds.md) | **The Step 2 structural thresholds: no minimum season size, max 26 episodes on either season, max 1,095-day gap.** 26 is the traditional full broadcast season and the cut is insensitive from 26 to 40 (1.1–2.4% of pairs); 22 was rejected at 196 shows and 13.8% of pairs, reaching into normal network drama. **The size cap is partly a cadence threshold** — 44 of its 51 shows are C4, the bucket where abandonment is most likely exposure-driven. Frame **1,226 → 1,138 shows, 220,107 pairs. Closes [0014](0014-no-content-filters-structural-fields.md).** | 2026-08-12 | Closed |
| [0021](0021-step5-contamination-gate.md) | **Step 5 contamination exclusion rule APPROVED** (gate 2 of 5). Exclude 16,665 pairs whose S2 evidence is entirely air-date-stamped, on a deterministic mechanism; exclude 1,542 with no S2 evidence and a fabricated binding clock start, on a censoring defect; **retain 201,900 of 220,107**, derive `W` on **128,099**. Four rulings inside the gate — **D1 keeps Step 1 §7 and does not reopen gate 1**, adoption 1 narrowed, adoption 2 re-ruled onto censoring, adoption 3 dropped. Two standing rulings outlive it: `W` derived on clean records and applied to all, and **liveness on insertion time**, making the play-`id` calibration a required Step 7 input. Red Team: four rounds, PROCEED at the fourth. | 2026-08-12 | Closed |
| [0022](0022-step5-rulings-written-into-task-sheet.md) | **The two Step 5 standing rulings are written into `task-sheet.md`.** They lived only in the decision log and the Step 5 artifact, while the specs the two isolated instances actually read said nothing about them — and **the dual-implementation diff cannot catch what the spec omits**, because both instances read the same silence. Step 6 now names the **128,099** estimation sample and its composition with D14; Step 7 names **insertion time** as the clock and the play-`id` calibration as a required input that **neither instance refits**. Found by `second-brain`. | 2026-08-12 | Closed |
| [0023](0023-step4-completeness-rule-upheld.md) | **The sweep-completeness rule stands as adopted.** Red Team returned HOLD and was **overruled on cascade cost, not on merit**: changing the tolerance restates the cohort, which re-runs the completer diagnostic, which moves the ≥50 candidate rule and the 1,138-show frame — against a correction worth **0.13 points**. Three findings become **Step 14 limitations**: leg 1 gates on `page_count`, which is `ceil(item_count/250)` and so derived from the header leg 2 exists to absorb; the discard is **not outcome-neutral** (+1.27 pts, CI [0.87, 1.66], direction **up**, compounding with the seeding and liveness biases); and a better instrument existed and was declined. Closes item 15. | 2026-08-12 | Closed |
| [0024](0024-w-is-the-90th-percentile.md) | **`W` is the 90th percentile of the C1 lag distribution**, not a curve-flattening judgment. Occasioned by Step 6 run 1, where two isolated instances agreed on every input — 128,099 sample, 25,120 C1 pairs, 689 negatives — and produced **`W` = 46 and `W` = 107** from the undefined word "flattens". The C1 density is close to scale-free past day 7, so the spec asked for a feature the data does not have. **Step 13's arms must span 46 to 107**, because fixing the wording removes the disagreement but not the dependence. | 2026-08-12 | Closed |
| [0025](0025-lag-unit-and-ceiling.md) | **The lag is a continuous instant difference and `W` is the CEILING of the percentile.** Run 2's two instances agreed to **fifteen significant digits** and differed only in unit — 107 vs 107.7135. Not a rounding preference: `watched_at < τ1` covers a pair iff its **fractional** lag is `< W`, so flooring is a systematic off-by-one against the operator. `W = 107` covers 89.976%, `W = 108` covers 90.020%. | 2026-08-12 | Closed |
| [0026](0026-step6-window-w-gate.md) | **Step 6 APPROVED. `W = 108 days`** (gate 3 of 5) — ceiling of the 90th percentile (107.7135) of the continuous lag on the C1 subset, 25,120 pairs of Step 5's 128,099 sample. Applies to all pairs. Four things travel with it: precision is **±18 days** show-clustered not ±8 iid; the percentile is imported convention, not selected by the data; **censoring is one-sided** (p90 rises to 213 at ≥8yr exposure, an upper bound); and D14's warrant is false. | 2026-08-12 | Closed |
| [0027](0027-step13-w-arms-above-the-adopted-value.md) | **Step 13's `W` arms extend above the adopted value, at 150 and 213 days.** Every mandated range topped out at `W = 108`, so the sensitivity tested `W` downward by a factor of three and upward not at all — while the censoring diagnostic runs one-sided upward (C1 p90 rises 107.7 → 119 → 128 → 146 → **213** with minimum exposure). **213 is an upper bound, not a rival estimate**; 150 is included so the response is traced rather than bracketed. Closes item 28. | 2026-08-12 | Closed |
| [0028](0028-step14-carries-every-routed-limitation.md) | **Step 14 carries every limitation routed to it.** Its checklist held **one** bias mechanism while **seven** were known and ~eleven limitations had accumulated across ten entries and four artifacts. Adds a seven-statement bias ledger — seeding down, liveness down, tail cap up, tolerance discard up, Step 5 population change up, Step 5 estimator bias down, larger `W` down — each with mechanism, direction and source, and **the instruction that they must not be netted**: two are not the same kind of quantity, same-direction pairs compound rather than confirm, and opposite-direction pairs do not cancel. Plus eight non-bias limitations. Same rule as item 23, different failure mode — Step 14 is single-implementation, so the risk is **plain omission**, not silent divergence. | 2026-08-12 | Closed |
| [0029](0029-step7-threshold-rule-and-w-propagation.md) | **`W = 108` propagated to its consuming steps; Step 7's threshold rule and Step 8's filter order fixed.** `W` appeared in neither Step 7 nor Step 8, while the two Step 6 artifacts state **107** and **107.7135** — a contradiction with two different wrong answers, into which two dual instances could have resolved differently. Step 7's *"set the threshold well beyond the normal gap"* withdrawn for a named percentile — **proposed at the 99th, not adopted** — with the gap as a **continuous insertion-instant difference** and the threshold rounded **up**. Step 8's filter order fixed, because the row set commutes but the required per-filter waterfall does not. | 2026-08-12 | **Open — the Step 7 percentile is proposed, not ruled** |
| [0030](0030-frame-field-corrections.md) | **The 2024/2025 contradiction corrected by addendum, and three frame field changes.** Step 1 relied on a 2024 cap that Step 2 had moved to 2025, and its horizon assumed `W ≤ 91` against the adopted 108 — **the gate is not reopened**, both figures corrected by post-approval addendum. Margin is **24 days**, zero shows lost, but censoring is **cohort-asymmetric** (2023–2025 loses 10.3% of pairs at `W = 213` against 2.7% pre-2020). **`show_network` dropped** — it errs in both directions and has no stable semantics; **`rating`/`votes`/`comment_count`/`subgenres`/`airs_day` added**, giving the frame a reception axis it lacked; **`size_quintile`** is now within-cohort rank, per-year having over-corrected worse than raw under-corrected. | 2026-08-12 | Closed |
| [0031](0031-the-50-completer-floor.md) | **The ≥50 S1-completer floor stands, and now has the warrant it never had.** The largest population rule in the study — 44,617 shows to 2,094 — and the only one with no entry and no sensitivity curve. Published: ≥25 gives +81% candidates, ≥100 gives −50%, and **there is no break in the distribution at 50**. Justified on cost, not on shape: shows near the floor are 42% of the frame by count but **15% by pairs**. **The frame at ≥25 is not computable without 1,699 API calls** and no claim is made about it. | 2026-08-12 | Closed |
| [0032](0032-step4-deliverable-regeneration.md) | **Step 4's deliverables regenerated at zero API calls; the pull stopped SAFELY, not cleanly; the resume cost restated.** `step4_run.json` and `step4_pull_log.json` were written by a `--max-users 3` run and reported **2,137 complete against 2,549** — stale by 464 decided users and 23,410 calls, because the record-writers fire only from `main()`'s `finally` and neither long run reached it. Fixed with `--max-users 0`, verified at an unchanged request-log line count. The pull's ledger, progress file and raw cache all held and nothing was lost, but `finished: false`, `stop_reason: null` and no exit line means **"exited cleanly" was never supported**. Resume costs **~70,000 calls and ~7.8 hours**, not 4.28 — that figure came from 8 users over 100 seconds, 123 of whose pages were free, and is user-count based so blind to the untouched users being the heavier half of every bin. | 2026-08-12 | Closed |
| [0033](0033-step8-per-air-period-censoring-counts.md) | **Step 8 reports retained-pair counts per air period after right-censoring, for every `W` arm.** The aggregate reads 97.6% retained at `W = 108` and hides that the loss is cohort-asymmetric — at `W = 213` the 2023–2025 cohort keeps **89.7%** against **97.3%** pre-2020, and survivors from recent titles are early adopters. **Closes the Product review's finding 5 ask**, though not the underlying thinness. Also disposes of the Engineering review's discard-rate residual: **bin 5 at +3 SD, no monotone trend, so sweep length is ruled out and the mechanism is unidentified** — recorded as a Step 14 limitation, not fixed. | 2026-08-12 | Closed |
| [0034](0034-step1-continued-boundary-amendment.md) | **Step 1 §7 amended: Continued is evaluated at `τ2 = ⟦T0⟧ + (W + H) = 199 days`; never-started stays at `τ1` = 108.** The old boundary scored a late completer as an abandoner. **2,246 pairs move, all one direction, monotone since `A ⊆ A_H`; the never-started share is unchanged; censoring cost is zero and no shows are lost; no new constant** — `H = 91` was already adopted by name at D10. It **fixes 39.5% of the misclassification and leaves 60.5% standing** as a reported residual. D3 is replaced by **D3′**, run at every Step 13 arm; Step 10's `p` uses `m_H`; liveness stays anchored at `τ1`; Step 8 gains the `A ⊆ A_H` invariant. **Eleven Red Team rounds; the rule was never broken and every hold was against the justification prose.** Ledger items **8, 9 and 10** go to Step 14 and publish together, never netted. **No stated ground exists for preferring `τ2` to first-S2-watch + `H`** — four were attempted and all four failed, and that absence is recorded. | 2026-08-12 | Closed |
| [0035](0035-agent-definitions-and-propagation-cleanup.md) | **The agent definition files are live spec and are amended to the decision log; Step 10 receives `0034`; two propagation errors and five stale expressions corrected.** `CLAUDE.md` points agents at their definition file first, and **ten decisions had propagated to `task-sheet.md` and none to `.claude/agents/`**. Both `data-scientist` halves — **byte-identical, and Step 7 is the next dual pair** — carried withdrawn liveness spec: logged events not insertion time, "well beyond the normal gap" not a named percentile, user-level not pair-level. **Identical halves in the wrong state produce a clean diff and a wrong answer**, which is the argument for treating these files as spec. Also fixed: Step 1's premiere anchoring, Step 6's flattening rule, Step 0's superseded 403 rule, Step 8's "fixed documented order" and its vacuous premiere invariant. `task-sheet.md` Step 10 gains `p` on `m_H`, the earlier-shifting direction, and the re-reported `p = 1.0` residual. | 2026-08-13 | Closed |
| [0036](0036-step7-threshold-and-shape.md) | **Step 7's liveness threshold is the 99th percentile, and the test applies to the gap bracketing `τ1` rather than every gap in the sweep.** At the 95th one ordinary gap in twenty trips the threshold; at the 99th it is one in a hundred, and a false-dead removes a pair against a bias that already runs down. **A whole-sweep test compounds the false-dead rate with the number of gaps** — at the 99th percentile an account with 50 gaps trips it **39.5%** of the time by chance, and it fails **selectively**, declaring dead precisely the users who log most. **No percentile fixes that; it is the rule's shape.** Liveness licenses trusting a null and the null is `\|A\| = 0` at `τ1`, so what must be established is that the account was alive **at that moment**. Edge cases fixed: no instant after `τ1`, or none at or before it, is **not live**, counted separately. **Closes README items 30 and 31 and unblocks Step 7.** | 2026-08-13 | Closed |
| [0037](0037-step7-reference-distribution-and-gap-unit.md) | **`0036` §1's basis withdrawn; the reference distribution becomes the bracketing-gap distribution; the gap unit and the namespace mechanism fixed.** §1's "one in a hundred" is true of a **uniformly drawn** gap and false of the gap the rule tests: the bracketing gap is **length-biased**, so **37.4%** of measured bracketing gaps exceeded the pooled-99th threshold — found by **both arms independently and in the same terms**. **§2, the shape ruling, stands** and was corroborated. Also recorded: **§2.1 understates the compounding by two orders of magnitude** — the median account has **8,247** gaps, not 10–100, at which a whole-sweep test trips with probability ≈ 1; and **the 38,696 "no instant at or before `τ1`" pairs are a clock mismatch**, `T0` on `watched_at` against liveness on insertion time, with the median pair's `τ1` **1,578 days before the account's first insertion instant** — routed to Step 14. **Gap unit fixed** at distinct insertion instants with an exact collapse rule, after the two arms read it 4 days apart. **Namespaces now assigned explicitly in the prompt.** | 2026-08-13 | Closed |
| [0038](0038-step7-frozen-spec.md) | **Step 7's spec frozen: reference population 152,126, one gap per pair, the quota and the inertness disclosed, the `W`-coupling accepted.** The rerun's arms had **no arithmetic disagreement** — A proposed 504 d, B proposed 914 d, and **each computed the other's figure to the digit**; the gap was one unsettled question, the reference population. **152,126 adopted on instance A's own counter-argument**: the 152,126 → 128,099 filter concerns the **first S2 watch**, which plays no part in `τ1` or liveness, while the lines above carry contaminated `T0` and `τ1` is built from `T0`. **Derivation and application populations must be identical** — A showed derive-on-128,099/apply-to-201,900 delivers **2.28% against a stated 1%**, the same defect `0037` withdrew. **The quota property is disclosed**: the level is set by the exclusion rate, not by any feature of the data. **The inertness is disclosed**: the measured-gap test does **3.45%** of exclusions and `0036` §2.3's edge cases **96.55%**, at every percentile from the 90th to the 99.9th. **`0036` §3's "derived independently of `W`" was unsatisfiable and is withdrawn**; Step 13 refits per arm and reports threshold and realised rate for each. | 2026-08-13 | Closed |
| [0039](0039-step7-liveness-gate.md) | **STEP 7 GATE APPROVED — the liveness threshold is 632 days**, 99th percentile of the bracketing-gap distribution on the 152,126, one gap per pair. **Gate 4 of 5.** **Both arms produced identical figures on every published number**, after three runs in which each divergence was a spec ambiguity closed by naming the operation rather than describing it. **Reported as 632 d with an account-clustered interval of [528, 787], never bare** — the i.i.d. interval [632, 645] overstates precision ~20×, since **34.4% of pairs share their bracketing gap with another pair** (largest tie group 298). Same treatment as `W`'s ±18 show-clustered interval. **Corrects `0038` §5**: 5.37% / 94.63% on the frozen population, and the invariance claim is **impossible** — the edge-case count is constant in `p` while the gap-test count is `100 − p` of 129,630, so the share ranges 36.5% → 0.4%. **Reference set is the 129,630 measured-gap pairs**: open-ended gaps are 3.17% of the extended set, so the 99th percentile over it would be infinite. **~79% of the no-instant-after bucket is censoring, not silence** — only ~880 accounts genuinely went dark. | 2026-08-13 | Closed |
| [0040](0040-step7-gate-reopened.md) | **STEP 7 GATE REOPENED on Red Team's HOLD; `0039` suspended.** **`0036` §2.3's second edge case is withdrawn — it contradicted an approved gate.** `0021` (gate 2 of 5) rules that any record inserted after the window closed **proves the account was alive**; every pair in the "no instant at or before `τ1`" bucket has instants after `τ1` by construction, so `0021` ruled them live while `0036` ruled them dead — on **76.8% of the filter's exclusions**. **The 18,250 are returned.** **Derivation moves to after D10**, which `0038` §2.1 had ranked as its first requirement and the run violated; the open-ended share then falls to **0.685%**, making the 99th percentile finite and **removing the need for the other edge case too**. **The conservative-direction argument is withdrawn** — bias 2 concerns accounts that *stopped* logging and is silent on accounts that *started late*, so it covered ~9% of the filter. **Four superseded figures were standing in `task-sheet.md` as operative spec**, including the impossible invariance claim `0039` had just corrected in `0038` but not in the file Step 8 reads. Two errors in `0039` itself corrected. **Step 9 must test whether the headline is sensitive across [528, 787]; if not, 632 is deleted rather than published.** | 2026-08-13 | Closed |
| [0041](0041-step7-extended-set-and-sensitivity-precondition.md) | **Reference set is the EXTENDED set, provisionally; NO threshold approved until the Step 9 sensitivity test runs.** Both arms proposed **1,293 d** and agreed on every structural figure — post-D10 **147,370**, classes **128,467 / 751 / 18,152 live**, not-live **1,282** down from 23,772 — diverging only 787 vs 790 on a bootstrap endpoint, inside their own measured seed noise. `task-sheet.md` line 266 still carried the suspended `0039` §5 restriction, **reasoning opposite to `0040` §2**; both arms flagged it and declined to reconcile it. **Open-ended enters as `+∞` — a construction ruling, and the thing that moves 632 → 1,293.** Degeneracy caveat carried: above the **99.4188th** percentile the percentile is itself infinite. **Derive/apply mismatch recorded, not repaired** — 1.4418% delivered against a stated 1%, routed to Step 14. **Sensitivity test at 787 and 2,200 d decides whether the threshold is deleted for the parameter-free rule.** | 2026-08-13 | Open |
| [0042](0042-step7-liveness-gate-parameter-free.md) | **STEP 7 GATE CLOSED — the threshold is DELETED and the rule has no free parameter.** A pair is **not live iff the account shows no insertion instant after `τ1`**; 751 pairs excluded from 166 accounts. A threshold was derived three times (632 d, 1,293 d) and deleted: across 787 / 1,293 / 2,200 d and parameter-free the shares move **0.026 / 0.038 / 0.012 pp**, ~3% of the clustered sampling width, and a 30–4,000 d sweep moves nothing beyond 0.243 pp. **`0041` §4's wording is withdrawn** — *"an instant at or before `τ1` and one after it"* reinstated the edge case `0040` §1 had just struck, and would have excluded **18,903 pairs from 1,434 of 2,402 accounts**, moving Continued −0.67 pp. **The filter as a whole moves the shares 0.027 / 0.016 / 0.011 pp against no filter at all**, so Step 9's bound is narrow because the filter is inert, not because the inference is tight — routed to Step 14. **Shares are provisional in population**: Step 8 applies liveness to 196,654 whose extra lines carry contaminated `T0`. | 2026-08-13 | Closed |
| [0043](0043-liveness-bias-sign-correction.md) | **Step 14 bias 2 had the WRONG SIGN, in the study's central honesty artifact.** The ledger said the liveness exclusion moves never-started **DOWN**; measured, the approved rule moves it **UP — 6.2055% → 6.2373%, +0.032 pp** — because it preferentially deletes **confirmed continuers**. Of the threshold rule's 1,282 exclusions: **1,079 Continued, 163 Started-and-left, 40 Never started**. Instance B had named it — *"the filter is not selecting on the outcome it was built to protect"* — and it was not acted on until Red Team made it blocking. **It offsets bias 1 rather than compounding it**, and **Step 9's bound bounds the wrong set**: six in seven excluded pairs have positive S2 evidence, so treating them as decliners is not conservative. Survived because `0040` §3's withdrawal never reached the task sheet. Also corrects **`0042` §4's 0.027 / 0.016 / 0.011 → 0.032 / 0.023 / 0.009** — those were the *deleted* 1,293-day rule's deltas, lifted from instance A and given a new subject. **Sixth instance of that error class.** Three of Red Team's four blocking items remain open; **the Step 7 gate is open and Step 8 does not launch.** | 2026-08-13 | Closed |
| [0044](0044-liveness-w-coupling-and-propagation.md) | **"No free parameter" WITHDRAWN — the liveness rule is fully determined by `W`; the rule propagated to the files Step 8 reads.** Deleting the threshold **made the coupling total** rather than removing it: PF-LIMIT's exclusion set **is** the open-ended bucket, a pure function of `W`, running **348 pairs at `W = 38` to 949 at `W = 213` — a factor of 2.7** — and **`W` was held at 108 for the entire sensitivity test that justified the deletion.** Recorded plainly: **the study deleted a parameter it had varied and handed the rule's authority to one it had not varied in the same test.** Honest wording everywhere is now *"no parameter of its own; fully determined by `W`"*, and **Step 13 reports the exclusion count per arm**. `0038` §6's threshold-refit requirement is **withdrawn** — it had no referent. **Propagation failure #6 closed:** neither `analytics-engineer` file stated the rule, and they are the first files the Step 8 instances read. Item 46 sharpened — **the surface is five files and an entry must name which it touched.** | 2026-08-13 | Closed |
| [0045](0045-liveness-bound-option-c-alt-rejected.md) | **PF-LIMIT kept, ALT rejected, the Step 9 bound restricted, `0043` corrected three ways.** Both arms measured ALT's exclusion set at **ZERO pairs** across every `W` tested — **Red Team's "on the order of 40" was read off the deleted 1,293-day rule.** The ordering obstacle was **not real** (row-local predicates commute; `0029`'s per-filter-sample-size rationale cannot reach position 7, which removes no rows), so ALT was rejected **on effect, not feasibility** — it buys a defence obligation for no effect. **Bound restricted to never-started exclusions: [16.7789%, 17.0355%]**, since all 751 exclusions have positive S2 evidence and 652 are confirmed continuers, which put the old ceiling **outside the feasible set**. **`0043` corrected: the sign is population-scoped — UP 0.032 on the derivation population, DOWN 0.192 on the application population Step 8 filters** — the "~40" remedy was **unexecutable at zero**, and "six in seven" merged two claims. **751 vs 1,355 disambiguated in all five files** so Step 8's correct count is not read as a divergence. | 2026-08-13 | Closed |
| [0046](0046-liveness-rule-alt-adopted.md) | **ALT ADOPTED — a pair is not live iff no insertion instant after `τ1` AND `\|A\| = 0`.** PF-LIMIT superseded: on APPLY it deleted **1,355** pairs, of which **604** rest on a null and **751 have directly observed outcomes** — 652 continued, 99 left — **with no warrant stated anywhere from `0021` through `0045`.** The second conjunct is the ruling: liveness licenses trusting a null and the null is `\|A\| = 0`. **`0045`'s ALT rejection is WITHDRAWN** — it cited the DERIV row where ALT is **zero by construction**, when ALT is exactly PF-LIMIT's never-started exclusion set, which `0045` §4.2 itself puts at **604 on APPLY**. **Bound republished [16.7146%, 16.9704%], width 0.2558 pp, one denominator, ceiling an identity**; `0045`'s [16.7789%, 17.0355%] mixed denominators and its floor was not a floor. Ordering settled — row-local predicates commute, position 7 removes no rows — with **line 6 now outcome-conditional** and monotonicity **non-strict**. **Five-file pass executed; propagation failure #7 recorded.** **New standing rule: every figure states its population at the point of use.** | 2026-08-13 | Closed |
| [0047](0047-step7-alt-rerun-corrections.md) | **`0046` §1, §4 and §7 corrected after the ALT rerun; D10 re-derived per arm; Step 8's monotone invariant is `>=`.** Both arms agreed on **every published figure** — 0 on DERIV, **604 on APPLY from 191 accounts**, per-arm 485–716, shares 16.7146 / 73.5221 / 9.7633, **100% never-started exclusions** — and both verified the ceiling identity **on integers**. They then refuted three claims in the entry that adopted the rule. **§1(a):** the 604 are **not** "exactly the pairs with no S2 record anywhere" — APPLY holds **23,260** and **22,656 stay live**; conjunct 2 narrows 196,654 → 33,373 and **conjunct 1 does the rest**. **§1(b):** the DERIV zero is **not forced by construction** — `has_s2` does not imply `\|A\| ≥ 1`, **9,145 DERIV pairs are never-started**, and four line-4 pairs satisfy both conjuncts at every arm but are removed by **D10**. **§4:** bound republished **[16.6633%, 16.9704%], width 0.3071 pp**, one denominator — **the third consecutive bound whose endpoint did not cover the case the filter exists for**, and a second standing rule is added for it. **§7:** too pessimistic — the rule **is** exercised on APPLY; only DERIV's diff is `0 = 0`. | 2026-08-13 | Closed |
| [0048](0048-liveness-rule-alt-broad-adopted.md) | **ALT-BROAD ADOPTED — not live iff no insertion after `τ1` AND NOT Continued.** The `\|A\| = 0` form stopped short of its own warrant. **Under `0034` only Continued rests on positive evidence**; Started-and-left is `\|A\| ≥ 1` **and NOT Continued** — the exit is a **null**. **Structural:** `τ2 > τ1`, so a pair silent after `τ1` can produce **no evidence in the `[τ1, τ2)` window the Continued test reads** and is scored "left" **by construction**. **`0046` §2's "751 directly observed" is corrected: 652 observed, 99 null-based.** Both of instance B's objections fail — the population-dependent sign is irrelevant and **choosing a rule for a stable sign is choosing the flattering number**, which B itself called an identity rather than evidence; and since `τ2 > τ1`, **ALT-BROAD at `τ1` is strictly narrower**, the conservative form. **Exclusions 99 on DERIV / 703 on APPLY**; **never-started bound unchanged** at [16.6633%, 16.9704%]; **the DERIV dual diff becomes 99 vs 99 — informative for the first time in this step.** A second bound, on started-and-left, is now required. **Propagation failure #8** fixed: five stale `task-sheet.md` lines including **332, which carried the superseded bound as operative instruction** while both `data-scientist` files marked it superseded. | 2026-08-13 | Closed |
| [0049](0049-step7-alt-broad-confirmed-and-six-corrections.md) | **Joint started-and-left bound adopted; six record defects corrected; calibration residual discharged.** **Both arms confirmed every ALT-BROAD figure** — 99 on DERIV / 703 on APPLY, per-arm 537–864, shares 16.7231 / 73.5592 / 9.7177 — and **both independently refused the narrow bound**, instance A naming it as what *"would have made this the fourth consecutive bound failing that exact test."* **S&L bound is [9.6830%, 10.0405%], width 0.3575 pp, over all 703**; the 99-only [9.6830%, 9.7333%] is a **labelled conditional sub-interval**, a factor of seven narrower. **Six defects:** `τ2 plays no part` is false under ALT-BROAD; `0046` §4's identity phrasing gives an unattainable 17.3279% and **Step 9 reads it**; ALT's movements quoted under an ALT-BROAD heading; "exclusion set empty on DERIV" false in five files; ALT's `→ 33,373 → 604` where ALT-BROAD gives `→ 52,514 → 703`; `0048` §5's series unlabelled, DERIV giving 147 at `W = 213`. **Residual discharged:** clamping **inert** — clamp value postdates every D10-surviving `τ1`, 0 of 66,961 excluded; **stable at the ~91% mass, unstable in the tail**; 700 of 703 survive a direction-only correction. **The S&L component is the fragile one** and routes to Step 14. **Agent-launch snapshot hazard closed** — the launch prompt, not the definition file, is the authority at launch. | 2026-08-13 | Closed |
| [0050](0050-step7-propagation-pass-and-channel-measurement.md) | **Six file defects fixed and each VERIFIED on disk; the residual limits routed to Step 14; the un-guarded channel measured.** Propagation failure **#9**, live in all five files, produced by `0048` and `0049` — including Step 14's bias-2 entry with **four clauses and four falsehoods**, ALT's decomposition eleven lines under an ALT-BROAD rule statement, and both `analytics-engineer` files saying **"Reporting 604 is correct"** beside **"EXPECT 703"**. **Both members of every pair carried each defect identically, so the dual diff shows agreement and cannot catch this class.** Limits routed because **recording a limit only in `decisions/` is not recording it**. **Channel measured: 297 pairs** — 207 NS + 90 S&L — so **ALT-BROAD closes 70.3% and leaves 29.7% open**, and the S&L bound treats those 90 as observed. | 2026-08-13 | Closed |
| [0051](0051-v1-v7-and-housekeeping.md) | **V1 and V7 fixed, nine housekeeping items cleared.** **V1 — propagation failure #10:** both `data-scientist` files ordered **ALT's superseded 485→716 series at Step 13** while line 68 of the same files carried the correct one and the task sheet forbade the superseded one. **Step 13 is single-implementation, so no diff catches it, and `CLAUDE.md` sends the agent to the definition file first.** **V7:** the two-ceilings sum is **100.3071%**, not 100.66% — **73.6537% matched no population**; Continued is **73.2962%** on 196,654 and **the excess is exactly 604/196,654, the same pairs counted in both ceilings.** The figure was taken from a review and propagated unchecked — **the failure `0046` §0's rule exists to prevent, committed in the entry that routed that rule into Step 14.** Housekeeping: `0050`'s index row; the gate checklist quoting **PF-LIMIT** two generations stale; items 30 and 31 struck; **item 46's count corrected from five to ten**; Step 14's bias-2 residue; `0034`'s *"`τ2` plays no part"* amended in the gate document that originated it; and **14 superseded Step 7 artifacts stamped**, including one carrying *"the exclusion set is empty on DERIV"* live in a public artifact. | 2026-08-13 | Closed |
| [0052](0052-liveness-rule-alt-matched-adopted.md) | **ALT-MATCHED adopted; `0051`'s V7 correction WITHDRAWN; five further defects fixed.** ALT-BROAD tested both nulls at `τ1`, but its own warrant holds for a pair silent after `τ1 + ε` for **any ε < 91 days** — the failure mode is continuous and it cut it at one end. **Each null is now tested where its own outcome is read.** Expected 703 → 793 exclusions, S&L component 99 → 189, **never-started bound unchanged.** **`0051` §2 was wrong: 73.6537% is the Continued CEILING** on (144,140 + 703)/196,654, **both arms publish it**, and the three-ceiling sum **100.6646%** is right — `0051` adopted a `second-brain` mislabel without checking the arms' JSON, **the failure `0046` §0 exists to prevent, in the entry correcting two other instances of it**, and left the task sheet pushing a Step 9 instance to delete a correct number. **Channel corrected to 52.4%** — the 207 never-started pairs were never in the gap, `0021` licenses their null. **This averts a fifth non-covering floor**: the 90 unclosed would have put the true S&L floor at 9.6373% against a published 9.6830%. **Propagation #12**: Step 9's two mandated sentences reached the task sheet and neither `data-scientist` file. **A-vs-B ratio reconciled at 45%.** **Population mismatch recorded** — on DERIV the point estimate lies outside its own bound. | 2026-08-13 | Closed |
| [0053](0053-0021-amended-two-windows.md) | **`0021` AMENDED for two windows — an amendment to an approved gate, not a clarification.** *"After the window closed"* was written when there was **one** window; `0034` created two, and the ruling has since read as *"after `τ1`"* **only by accident of when it was written.** **Amended reading: an insertion after the window FOR THE QUESTION BEING ASKED proves the account was alive for that question** — `τ1` licenses the never-started null, `τ2` the started-and-left null. **`0048` §9's "insertion after `τ1` ⟹ live" is WITHDRAWN.** Stake: **90 APPLY / 89 DERIV exclusions show an insertion after `τ1` — 47.3% of DERIV's whole exclusion set** — all forced live under the withdrawn gloss, and **0 under ALT-BROAD**, which is why it surfaced only now. **Both arms confirmed all three of `0052`'s expectations** (APPLY **793** = 604 + **189**; never-started bound unchanged; **DERIV 188**, previously unmeasured) and **ALT-MATCHED closes 90 of 90 by index equality**, leaving **no residual channel by construction**. **Nine defects fixed**, my floor and population figures corrected (9.6372%, 6.2134%), and **the ceiling excess mechanism refined**: each NS exclusion sits in all three ceiling numerators and each S&L in two — 2×604 + 189 = **1,397 pairs = 0.7104 pp**. Two limitations routed: **the added 90 sit closest to their boundary** (median margin 44.5 d vs 202.5) and **D10 admits `τ2 = τ_pull`**. | 2026-08-13 | Closed |
| [0054](0054-revert-to-alt-broad-widen-the-floor.md) | **ALT-MATCHED REVERTED; ALT-BROAD restored with the S&L floor widened to 9.6372%; `0053` WITHDRAWN entirely.** **The rule change bought nothing:** on all three identified sets ALT-BROAD-with-a-covering-floor and ALT-MATCHED are **numerically identical** — S&L [9.6372%, 10.0405%], Continued ceiling 73.6995%, never-started unchanged. It moved only point estimates, and cost an amendment to an approved gate, a contradiction with `0034`, a fragility transfer and a nine-defect wave. **`0053`'s premise was false**: `0034`, the entry that *created* the second window, ruled *"Liveness stays anchored at `τ1`"*, and `0051` re-affirmed it with both windows in view — so `0053` amended `0021` while leaving the one ruling that forbids the re-anchoring **standing and uncited**. **`0052` §4's rejection was backwards** — widening to 18,952 is what makes the endpoint covering. **The warrant was false for the pairs it targeted**: a record at `s` carries any `watched_at ≤ s`, and the 90 have p5 margin 1.7 d, min 0.13 d. **The continuity argument is symmetric — it proves no instant in the interval is warranted.** **Sweep run:** exclusions rise 703 → 793 smoothly across `τ1 → τ2` with **no elbow and no natural cut**. **Propagation #13 fixed** — both `analytics-engineer` files carried EXPECT 793 and EXPECT 703 ten lines apart, and Step 9 never got `0053`'s pass. Two A/B divergences **reported not reconciled** (792 vs 791; 0.4032 vs 0.4033), and B's 52.7% ratio **withdrawn as a rounding artifact**. | 2026-08-13 | Closed |
| [0055](0055-deriv-floor-widened-and-the-grep-control.md) | **The DERIV S&L floor is widened to 16,655 / 147,370 = 11.3015% and the DERIV Continued ceiling to 121,570 / 147,370 = 82.4930%** — **`0054` §1 asserted numerical identity "on all three identified sets" and implemented it on APPLY only**, while both figures sat unused in the arms' own ALT-MATCHED deliverables. Uncorrected, Step 9 publishes a DERIV floor **0.0604 pp above the case the filter guards against** — the sixth consecutive non-covering endpoint, created by the entry written to stop the fifth. **The floor's margin argument is WITHDRAWN as cherry-picked**: p5 = 1.7 d was quoted, the median for the same 90 is 44.5 d. **A floor is a worst case, not an expectation — admissibility sets an endpoint, plausibility does not enter**, so both statistics are inadmissible. Measured anyway: the endpoint **moves** 0.0458 pp APPLY / 0.0604 pp DERIV, and **the widening is one-sided — the ceiling does not move.** **New standing control in `CLAUDE.md`: SEVEN propagation surfaces, and READ-BACK PLUS GREP** — read-back proves the new text landed, only grep proves the old text is gone, and three consecutive failures were files where both were true at once. `artifacts/` and `second-brain` memory were never checked; **the dual diff cannot catch propagation at all.** **#14–#17 fixed** — including both `data-scientist` files calling the **adopted** Continued ceiling *"ALT-MATCHED's, withdrawn"*, and two Step 14 bullets that are **false** under the restored rule. **Three defects in `0054` corrected:** its identity table was unlabelled, it published B's **0.4033** rounding artifact four paragraphs after withdrawing another, and its *"every figure is already on record"* is **false for 18,952** — no arm had reproduced the adopted bound, so both arms now verify it. | 2026-08-13 | Closed |
| [0056](0056-derived-figures-and-the-dependency-list.md) | **The four figures DERIVED from the started-and-left floor are corrected, and the dependency list that should have caught them is written.** `0055` moved the floor and left behind the **conditional sub-interval** — now **[9.6372%, 9.7333%], width 0.0961 pp**, because **the conditioning constrains the 604 and says nothing about the 90** — the **attainable-corner table** (floor corner 9.6372%, Continued **73.3924%**), and **both sampling ratios** (0.47× and 6% → **50.9%**; B's 6% understated the systematic range **8×**). **Same failure as `0054`, one entry later.** **`9.6830` is REMOVED from the false-positive register** — it has no legitimate reading, and **registering a string exempts it from the grep control**, so the exemption was granted to the one string the control most needed to catch, on four surfaces, in the section that created the control. **The artifact stamp is rewritten:** *"everything else stands, including every point estimate"* was **false for three figures it did not name, all three derived from the floor it announced had moved** — propagation **#19**, the first found on surface 6, found inside the fix for surface 6. **New standing control in `CLAUDE.md`, `## Derived figures`:** each endpoint carries a written list checked **as a set** whenever it moves — four items for the S&L floor, three for never-started, three for any ceiling, **per population**. `0055`'s three counting defects fixed, including a **third** asserted-but-not-taken action in one entry. **The `0021` warrant** for the 207 never-started channel pairs added where the bound is published. **The bootstrap is recorded as a Step 9 blocker** — arms diverged on `B`, seed AND statistic, and `0052` §6's *"now specified"* is **struck**: the string appears **zero** times in every file an agent reads. | 2026-08-13 | Closed |
| [0057](0057-json-halves-transitive-lists-and-the-open-window.md) | **The `.json` halves are corrected — the machine-readable half of the operative deliverable certified the numbers its own prose withdrew.** Both still carried the FIRST stamp and `"everything_else_stands"`, the sentence `0056` §4 ruled false; the positive grep returned **zero** for `0.0961`, `73.3924` and `0.509` in both. **Fifteen values corrected per file**, matched on **key as well as value** so the three `19042` POINT ESTIMATES were not corrupted. **`0056`'s "each is marked inline" was false and is now actually done** — the fourth asserted-but-not-taken action in three entries, inside the correction for the third — and **bounded**: `0.0672%` and `0.3575%` as shares of population are legitimate and deliberately unmarked. **The dependency lists close TRANSITIVELY** — the floor moves the Continued ceiling, which moves the three-ceiling sum and the excess count, and **`1,307 / 100.6646%` was #16, two hops out: the list written to prevent #16 did not reach it.** Four items added (bound width, sub-interval ÷ sampling ratio, the per-`W` series with Step 13 as consumer, any width-ratio — `bb-b`'s "factor of 7" is **4.2**). **`0.0503`, `0.3575` and `73.3466` registered** — the three still-live strings `0056` corrected and did not register. **The channel window is fixed to `(τ1, τ2)`, OPEN** — settled by the adopted warrant, not ambiguous: at `s = τ2` the unobserved remainder is empty. Fixed now because at `W = 213` D10 puts `τ2` at `τ_pull`, **where a mass point sits**; the script now **asserts** inertness rather than assuming it. **Surface 7 is the DIRECTORY**, not the glossary. **U2 to Step 14: measured and non-zero at three (#19, #20, #21); 18 is a surfaces-1–5 count and must never be published as a total.** | 2026-08-13 | Closed |
| [0058](0058-regeneration-replaces-hand-patching.md) | **Hand-patching stops.** Four consecutive entries corrected these artifacts by patching individual values, and every Red Team finding in reviews 9–11 was a value a patch reached in one file and missed in another. **`0057` §1's "fifteen values corrected and verified individually" was false** — it matched a whitelist of key names, so the **corner table**, the whole **`sampling_error`** block and the **per-`W` series** were walked past; **the one line that moved was the ratio, and the patch missed both its operands.** **`src/step7_regenerate_derived.py`** now writes every derived figure into both halves of both arms from **one expression each**, lists absent paths rather than skipping silently, declares the per-`W` series scope-limited **in its key name**, and makes it impossible for a still-live value to enter the superseded list. **Stamps are NEGATIVE ONLY** — a stamp naming the corrected value guarantees the positive grep passes whether or not the body was fixed. **`src/check_surfaces.py` replaces grep**, matching numerically at both precisions: textual grep cannot see the JSONs, since `9.6830` is not a substring of `9.682997`, and **every value that survived review 11 was one whose registered form rounds up.** It found **22 unlabelled values grep could not** — including the **reverted rule's own deliverables carrying no stamp at all** and sixteen JSONs with no `_SUPERSEDED` key — and correctly refused two false positives. **Finding 3 REVERTED: `0057` wrote arm b's denominator into arm a's file.** The conventions differ (a: floor endpoint's own CI; b: the under-the-rule point estimate's), the spec fixes neither, and **the never-started ratio was correctly left divergent in the same files, which is the proof.** Both denominators are now named inputs and an assert fails if they are ever equated. **Dates corrected across 49 files with a note in each entry** — `0052`–`0057` were dated tomorrow. Step 14 gains the **commutation caveat**: the check shows the two filter orders agree on observed COUNTS, not that the estimand is unaffected. | 2026-08-13 | Closed |
| [0059](0059-one-register-no-file-exemptions-the-quotient.md) | **B1: the regeneration inverted the failure it was written to end.** `CLAUDE.md` puts the bound ÷ sampling-width ratio on both dependency lists and `json_targets()` declared neither, so the script **wrote both operands and left the quotient** — `0.509` (arm b's denominator) sitting in arm a's file between `0.403246` and `0.7602`, whose quotient is **0.5304**. `0057` reached the ratio and missed both operands; the regeneration reached both operands and missed the ratio. **Four paths added, each divided by that arm's OWN denominator**, the never-started denominators named too, and the hard-coded `..._is_retained_and_marked_superseded: True` — which nothing checked and which was false — **replaced by an assertion that re-reads the written file.** **B2: the whole-file exemption is DELETED.** A stamp in the first 45 lines exempted **19 md and 16 json files — the entire artifact set, including both OPERATIVE deliverables** — so `0058`'s "22 values found" were closed by exempting 36 files, not by labelling 22 values, and `CLAUDE.md` requires the string be named superseded **at the point of use**. Files are now exempt **by name, with a reason, 33 of them**, and **the operative pair is not exemptible, in code**. Regexes tightened after, `CONTEXT` 6 → 2. **Re-run honestly the count is 12, not zero.** **B3: one register, `src/step7_register.py`, SCOPED BY FILE** — `0.509` is arm b's correct figure and arm a's wrong one, which a flat register cannot express and which is why it survived two reviews. **The positive half now covers this chain's own corrections** (0.5304 / 0.1309 / 0.5090 / 0.1310, checked by nothing before). Absent target paths now **fail** — 28 in arm b were declared derived and never written at exit 0. **The md bodies are verified**, where B1 lived. **Instance A's D-2 actioned:** `step7_floor_extremes.py` hardcoded its conclusions as asserts so it could confirm and not refute; expectations are now data, **10/10 CONFIRMED**, and the `W = 213` claim is labelled **ARGUED, NOT MEASURED**. **`0058`'s `LIVE_ELSEWHERE` claim WITHDRAWN — it never fired**: the filter compared against a list that never held the values, so the property was asserted, not structural. | 2026-08-13 | Closed |
| [0060](0060-the-never-started-quotient-and-the-scoped-exemptions.md) | **B4: `0059` created four ratio target paths and asserted one.** The never-started quotient was written by the script and **checked by nothing — the identical structure to the hard-coded literal `0059` had just replaced** — so `bb-a.json` carried **0.2818** while every prose surface said **0.2813**, and neither was in the register. **`0.2813 = 0.307138 / 1.092`, and `1.092` is the UNDER-THE-RULE CI — arm b's convention**, so arm a was running two conventions in one six-line block. **Ruled: arm a takes the floor-endpoint CI, 0.2818**, matching its own S&L convention; both registered; `check_ratios_written()` extended; the two DERIV ratios are **0.0 by degeneracy and deliberately unregistered, with the reason stated.** **The "proof" sentence is WITHDRAWN from three places** — `0.2813` against `0.27211` was **one convention on two bootstraps**, and I published it twice as evidence of correct divergence when it was **itself an instance of the defect it certified**, the same failure as adopting 73.6537% without checking its population at `0051`. **B5: `0059` §2 asserted a `MARK` property the code lacked** — the edit was a string replace that failed silently with nothing asserted, the **third consecutive blocker of that shape**; corrected, and the correction now **asserts itself at import**. Two dead escaped-dot alternatives removed. **B6: the `EXTREME_NONE_READINGS` branch was UNREACHABLE** — the general `DECLARE` already matched its phrase — so the exemption was disarming the control for `0.0503`, `0.4033`, `0.4703` and the scoped ratios. General branch deleted; `DECLARE` **scoped by file AND value** to the verification arms' deliverables, JSON path markers separated from prose. **B7: the successor rule now runs on the EMITTING LINE** — on the block it was already satisfied, since `0.403246` is within tolerance of `0.4032` and self-declared `0.4033` two lines away. **Known limit recorded:** both controls walk numeric leaves only, so a superseded figure inside a JSON **string** is invisible to them. | 2026-08-13 | Closed |
| [0061](0061-withdrawn-claims-are-emitted-by-the-generator.md) | **B8: the withdrawn "proof" sentence was struck in the three places a HUMAN typed it and left in the one place the SCRIPT types it**, so the generator wrote it back into all four operative deliverables on every run — `bb-a.md` **asserting it at line 77 in the block headed "GENERATED, do not hand-edit" and striking it at line 316 in hand prose**, with the generated block winning on regeneration. **This is `0058`'s own failure mode arriving from the other direction:** the generator exists because hand-patching reaches one file and misses another; here the hand-correction reached three files and missed the generator. Removed from **all three writers** — `.md`, `.json` `_DERIVED`, and the source comment that gave the withdrawn claim as the reason `NS_RATIO_DENOMINATORS` exists — regenerated and confirmed gone. **Both controls were structurally blind**: the `.md` form carries no numbers and the `.json` form is a string under `_DERIVED`, which `verify()` skips. **`0060` §6 recorded that gap as "not a defect today" and it was ALREADY a defect on the day it was recorded**, live in four files. **The register now holds WITHDRAWN PHRASES as well as values**, and `check_surfaces.py` gains a third half scanning `.md` text and JSON strings. **Its first run found B10** — the `.md` writer still emitting the hard-coded literal `0059` had removed from the JSON half — fixed. **B9 is NOT closed:** `check_ratios_written()` returns without checking anything for arm b, so half the dual is unverified and `bb-b.json` still carries `0.403245` against the adopted `0.403246`. | 2026-08-13 | Closed |
| [0062](0062-a-check-that-looks-nowhere-must-fail.md) | **B9: `check_ratios_written()` skipped arm b in full and reported OK.** Arm b stores its bootstrap under `bootstrap`, which `ABSENT_OK` allowlists as absent, so the loop `continue`d on both populations, **checked zero rows and returned an empty failure list — which reads identically to everything being right.** `0060` §1's coverage claim was **true for arm a only**; arm b's four ratios were never regenerated and `bb-b.json` carried `bound_width 0.403245` against the adopted **0.403246**. **`RATIO_LAYOUT` now declares where each arm actually stores its ratios, a missing path is a FAILURE not a skip, and the function fails if it checked zero rows** — 12 rows arm a, 18 arm b, printed. **Every allowlist and skip path in all three scripts audited; four more of the same shape found**: parse failures returning `[]`, the input cross-check skipping an arm silently, allowlist entries that never fire, and surface 7 globbing `*.md` so a `.json` there was outside the control entirely. **The generator now holds ONE definition per statement and per figure** — B8 and B10 were one literal in two writers — **and the two halves are COMPARED off disk**, 72 figures and 8 statements per arm, failing on comparing zero. **`LEGITIMATE` consulted** (conflict fails; an unused row is reported); **`ADOPTED_IN` distinguishes `.md` from `.json`** — all six ratios now confirmed in both, where before `frag in f` could not tell them apart; **`step7_floor_extremes.py`'s last `assert` is a compared row**, 11/11. **The covering qualifier — "covering with respect to insertion-dormancy, exhaustively; open only across channel classes D4 and D9" — propagated to all eight surfaces** after six entries living only in `decisions/` while Step 9 publishes the bound it qualifies. **The control apparatus could not have caught that: a missing qualifier is neither a wrong number nor a withdrawn claim.** | 2026-08-13 | Closed |
| [0063](0063-widths-from-counts-and-the-rule-objection-measured.md) | **The rule objection is CLOSED ON MEASUREMENT.** Red Team's fifteenth review moved off the record and onto the rule for the first time since review 8: conjunct 2 makes liveness outcome-conditional, and the alternative *"nobody has priced"* is to drop it. **Measured — one line, zero API calls: `cont ∧ silent` = 652 on both populations**, taking APPLY 703 → **1,355** and DERIV 99 → **751**. **Those are PF-LIMIT's own counts, and `0045` §1 prints its DERIV split as 751 = 0 + 652 + 99.** So the premise fails — **PF-LIMIT IS the no-conjunct-2 rule**, adopted `0041`, superseded `0046` — and the pricing settles it: dropping conjunct 2 excludes **652 Continued pairs on evidence they demonstrably produced**, which is the one thing liveness cannot coherently do, since **Continued is not a null.** **What survives is a number:** the outcome-conditioning is **652**, now measured rather than argued, and it publishes with the Step 14 limitation. **2.5 fixed:** every width was a **difference of two 6-dp rounded percentages** — the exact construction `SUPERSEDED[0.4033]` names as a rounding artifact — so the **same 793 pairs on the same 196,654 gave 0.403246 and 0.403247 in one generated block**, and `0062`'s corrected `bb-b` value landed on the exact form **by coincidence.** Widths are now `(ceil_n − floor_n) / n`, with asserts that each width is its own count over the denominator and that the S&L and Continued widths are equal. **Everything else from review 15 is LOGGED AS OUTSTANDING, not fixed** — 2.1's self-comparison, 2.2's four unchecked ratios, 2.3's write-only `_DERIVED`, 2.4's five wordings, 2.6's `LEGITIMATE`, seven smaller items and DF-3. **Reviews 1–8 contested the rule; 9–15 found propagation and control defects in figures derived from an unchanged rule.** | 2026-08-13 | Closed |
| [0064](0064-step7-gate-approved.md) | **STEP 7 APPROVED. GATE 4 OF 5 CLOSED.** ALT-BROAD: not live iff no insertion after `τ1` **AND** not Continued; silence anchored at `τ1` and only at `τ1`; channel window `(τ1, τ2)` open. Exclusions **703 APPLY / 99 DERIV**. S&L **[9.6372%, 10.0405%]** APPLY and **[11.3015%, 11.4291%]** DERIV; Continued ceilings **73.6995%** and **82.4930%**. **Covering with respect to insertion-dormancy, exhaustively; open only across channel classes D4 and D9.** **APPROVAL IS UNCONDITIONAL — the residual is open and PUBLISHED, not resolved**, and the Human Lead confirmed that framing rather than accepting it as the drafter's. **The bootstrap's "blocks Step 9 not Step 8" placement is recorded as RED TEAM'S RECOMMENDATION, not a ruling.** **Fifteen Red Team reviews, fifteen HOLDs — but reviews 1–8 contested the RULE and 9–15 found propagation and control defects in figures derived from an unchanged rule**, none of which altered the rule, the population, the exclusion counts or any bound endpoint. Uncontested from review 5; arithmetic independently recomputed and confirmed at reviews 12, 13 and 15; the last substantive challenge closed on measurement at `0063`. **Step 8 may launch. Step 8 is the remaining gate.** | 2026-08-13 | Closed |
| [0065](0065-dates-corrected-and-the-third-blindness-class.md) | **The dates on `0060`–`0063` are wrong, not the approval.** Corrected to **2026-08-13** across 22 files with a note in each, as `0058` §6 did; **the Step 7 approval stands at 2026-08-13 and no longer predates `0063`**, which carries the 652 the approval rests on. **`0060`'s opening parenthetical is struck** — it asserted the 14th was *"correct"* while taking the date from the clock rather than the working day, which is `0058` §6's drift with the sign flipped, in the entry citing `0058` §6 to justify itself. **T1 logged as a THIRD BLINDNESS CLASS:** `second-brain` carried `0054` §3's *"p5 margin 1.7 days, minimum 0.13"* as live reasoning in three files for nine entries after `0055` §2 withdrew it. **Neither control could ever have seen it** — `1.7` and `0.13` are **correct statistics** withdrawn only as **grounds**, so the numeric half has no superseded number to match, and the argument travels **paraphrased**, so the phrase half has no stable string. **A withdrawn ARGUMENT built from CORRECT STATISTICS is recognised by what it claims, not by its digits or its text.** **No control is built for it** — that would be a prose checker, and three controls in six entries have reported clean while checking nothing. **What stands in for one:** an entry that withdraws a **ground** rather than a **figure** says so in the withdrawn-claims register and names the statistics still true but no longer load-bearing. Found by a reading agent, like the covering qualifier's absence at `0062` §4. | 2026-08-13 | Closed |
| [0066](0066-step8b-output-schema-propagated.md) | **Step 8b, output schema, propagated** — defined in a prior session and **present in neither `task-sheet.md` nor either agent file.** Added between Steps 8 and 9 and to both `analytics-engineer` files identically. **Owner: Analytics Engineer. Mode: Chained. Review: Engineering.** **Amendment 1: the key is `W` ALONE** — the original said *"combination of `W` and liveness threshold"*, and there is no threshold: one was derived three times (632 d, 1,293 d) and **deleted** at `0042`, the rule being **parameter-free**. *(`632` is also the legitimate frozen-D10 never-started component at `W = 125`.)* **Amendment 2: TWO bounds, not one, on BOTH populations.** Never-started has floor and ceiling and **no conditional sub-interval — structurally**, since the sub-interval conditions on that bound's own exclusion set; **recorded as inapplicable rather than omitted, because an absent field and an inapplicable one must not look alike**, and its DERIV bound is **degenerate** so a zero-width bound must not read as missing data. Started-and-left has floor, ceiling **and** the sub-interval, whose conditioning constrains the **604** not the **90**; **on DERIV it coincides with the bound and coincidence is recorded as a measured fact.** **The Human Lead's phrasing gave never-started a sub-interval it does not have; implemented correctly and flagged.** Four consequences of later rulings written in: **the Continued ceiling and the three-ceiling sum**; **the scope qualifier as a FIELD with D4/D9 slots**; **both arms' values where the arms legitimately differ**, since one slot per figure would force the ratio reconciliation the spec forbids; and **the bootstrap settings per CI**, so an unfixed spec is visible rather than silent. **"No conversion layer" is the load-bearing line** — a conversion layer is a second definition of every figure, which is the defect this study has hit most often. **Not launched.** | 2026-08-13 | Closed |
| [0067](0067-0064-propagated.md) | **Propagation failure #22, in the APPROVAL entry itself.** `0064` approved the Step 7 gate, `README` was ticked, and **the approval reached no file an agent reads** — `CLAUDE.md`'s *"recorded only in `decisions/` is not recorded"*, broken where it most obviously applies. `task-sheet.md:229` said **"NOT approved; seven Red Team HOLDs"** (it was fifteen); the **Gate summary had Steps 5, 6 AND 7 unticked**, 5 and 6 wrongly for two days; both `analytics-engineer` files said the gate was OPEN; **`0064` reached neither `data-scientist` file.** **Found independently by BOTH read-back instances, before Step 8 launched.** **Two more stale lines fixed:** `:258` blessed the superseded rule's answer — *"reporting 0 on DERIV and 604 on APPLY is correct and the two are not a divergence"* — **inside the bullet withdrawing ALT**, where the adopted answer is **703/99**; and `:96` still carried **"on a 403, hard stop and report,"** amended in `CLAUDE.md` on 2026-08-10. **The Gate summary is prose about state carrying no figure and no withdrawn phrase, so no control covers it.** **The four judgment items the read-back raised are untouched.** | 2026-08-13 | Closed |
| [0068](0068-waterfall-line-1-defined.md) | **Waterfall line 1 is the S1-completer population, 220,107.** The spec named no base and **four defensible readings sit on disk** — 2,900,762 / 278,452 / 274,741 / 220,107 — with **lines 1, 2 and 3 all moving with the choice**, so two faithful instances would have reported different waterfalls on an identical table: **the failure the fixed filter order exists to prevent, one position upstream of where that fix reached.** Lines 2 and 3 now follow from line 1 and **no instance chooses a base.** *(Not resolved: instance A measured 220,103 if D11 is applied at position 3 as Step 1 requires.)* **§2 is a SURVEY, reported and NOT resolved**, of everything else Step 8 names without defining at the point of use — **seven with one correct answer already in the record** (the 3,440's population; the 703's denominator; "the sample"; whether `\|D\| ≤ L` is a code check, where the two arms diverged 4-vs-6; D3′'s denominator; a *"line above"* that does not exist; `>` vs `≥` at `τ1`) **and eight needing a ruling** (both-channel users, 324 of 5,694; `action` at pair grain; D2's 168 both-terms-binding ties; the drop-share denominator; **DERIV, which no Step 8 position produces**; **D4, which has no count at all**; the `τ_pull` evidence scope that already produced the 792/791 split; and the censoring order). | 2026-08-13 | Closed as to line 1 |
| [0069](0069-seven-spec-gaps-closed.md) | **The seven Step 8 gaps with one correct answer are stated at the point of use.** The **3,440** is on the **uncensored 128,099 estimation sample, not Step 8's population** — which is why Step 14 calls it a floor; the **703** is on **APPLY = 196,654, the position-5 output**, and 99 on DERIV; **"the sample"** is the **post-position-7 row set**; **D3′'s share** is on the population and arm named at the point of use; the **97.6%** is restated with its source **because there was no aggregate line above stating it**; and the **silence test is STRICT** — an instant exactly at `τ1` does not make an account live. **Every invariant is now labelled CODE CHECK or DATA CHECK**, resolving the read-back arms' **4-of-6 against 6-of-6** split: **four pure code checks, one that is a code check by construction and a real cross-check only because the S1 completion date must be recomputed INDEPENDENTLY, and the 703 expectation, which is not an invariant but a population reconciliation.** Neither arm was wrong; the spec had labelled exactly one. **Four of six assertions cannot fail on any data, and "all invariants passed" overstates what was verified unless it says so.** | 2026-08-13 | Closed |
| [0070](0070-eight-step8-rulings.md) | **Eight Step 8 rulings.** **Step 8 emits BOTH populations** (196,654 and 147,370) **and the D4 count** — omitting either forces something downstream to rebuild it, **a second definition of one figure, the defect this study has hit most often**. **The silence test's evidence is restricted to records before `τ_pull`** — **applying D11 consistently, not a new ruling**, since D11 makes `τ_pull` a global frozen cutoff and the silence test is a computation; **measured before ruling and inert on the approved gate, 703 and 99 either way**, because no instant exceeds the clamp. **Discovery channel becomes two booleans** (324 users in both, and Step 11 tests exactly that bias). **`action` becomes per-pair counts by type** — it is record-level while the row is a pair, and Step 1 already ruled it a property of the **logging client**, not an outcome variable. **D2 splits three ways** — 168 pairs have both `max()` terms binding and **a tie is its own category, not a tiebreak**. **Drop denominator is position 5 = 33,373** with post-liveness 32,769 alongside. And **the filter order is KEPT while the percentage moves: 10.3% → 10.5%** — the order was set at `0029` on the ground that censoring is objective, the 10.3% predates it, and **changing a filter order to preserve a published percentage is backwards**. **Routed to Step 14: four of six assertions cannot fail on any data, so "all invariants passed" overstates what was verified unless it names which could have failed.** **Named as NOT propagated: rulings 1, 2 and 7 change what Step 9 receives and neither `data-scientist` file has been told** — left undone, Step 9 builds the second definition those rulings exist to prevent. | 2026-08-13 | Closed |
| [0071](0071-data-scientist-pass.md) | **The `data-scientist` pass `0070` §5 named as not done.** Rulings 1 and 7: **Step 9 CONSUMES Step 8's DERIV and D4 and rebuilds neither** — *"if Step 8's output does not carry them, say so and stop."* **The load-bearing sentence: a reconstruction that agrees today is still a second definition tomorrow, and the dual diff CANNOT SEE IT, because both instances would rebuild the same way** — agreement would be read as verification. Ruling 2: **the silence test's evidence is restricted to records dated before `τ_pull`**, stated where the rule statement lives; **applying D11 consistently, not a new ruling**, and **measured inert on the approved gate — 703 and 99 either way** — so it bites on the robustness tail, which is where the 792/791 split lived. **Pair verified byte-identical apart from `name:`.** **Named as still unstated: `task-sheet.md` Step 7's `THE RULE` block**, which **Step 13 reads across eight arms** — the scope now lives in two of the three places the rule is written down. | 2026-08-13 | Closed |
| [0072](0072-rule-scope-in-all-three-places.md) | **The last gap closed: ruling 2's evidence scope reaches `task-sheet.md` Step 7's `THE RULE` block**, so the silence test's `τ_pull` restriction now lives in **all three places the rule is written down** rather than two. **Not cosmetic — Step 13 reads that block and re-runs the rule across eight `W` arms, and an asymmetry between two written copies of one rule is exactly what produced the 792/791 divergence**, where neither arm was wrong against the text it read. The strict reading of *"after `τ1`"* is stated in the same block for the same reason. **Step 8 LAUNCHED as a dual pair, namespaces `a` and `b` assigned explicitly in the prompt** (`0036` recorded a collision when instances inferred their own), **both pointed at `task-sheet.md` Step 8 itself with no paraphrase and no spec file** — writing one would have been **a second definition of the step, the defect rulings 1 and 7 exist to prevent, committed at the moment of launching them.** **It is a gate: each instance produces and stops.** | 2026-08-13 | Closed |
| [0073](0073-comparator-and-ruling-4-completed.md) | **The censoring comparator is 3.0%, not 2.7%.** `0070` moved the 2023–2025 loss to the mandated order and **left its comparator on the superseded one**, so one sentence carried two orders — **the narrowest instance yet of a correction reaching the figure it aimed at and not the figure beside it.** **Both Step 8 instances found it independently.** Corrected in `task-sheet.md` Steps 8 and 14 and **marked, not rewritten, in the approved Step 1 deliverable.** **`0070` ruling 4's propagation is completed:** three surfaces still required the `action` column it replaced — `task-sheet.md` Step 13, the `analytics-engineer` head bullets **which contradicted ruling 4 further down their own section**, and Step 1 §9's hand-off list. **Step 13's arm is unchanged; only what it reads is** — `checkin`-only iff that count is positive and the other two are zero. **Instance A found all three, including the contradiction in its own definition file, and emitted counts anyway** — reporting the defect rather than following it. **Neither Step 8 proposal is adopted; the four divergences and every open item stand.** | 2026-08-13 | Closed |
| [0074](0074-six-step8-rulings-and-the-eighth-surface.md) | **Six rulings on the Step 8 dual run, and `processed/` becomes the EIGHTH propagation surface.** **Table grain: position 5, 196,654 rows, `live` and `outcome` as COLUMNS** — both readings gave **identical counts** (195,951 × 86 against 196,654 × 87), so this is a ruling not a correction, and it is **rulings 1 and 7's consume-don't-rebuild applied to the row set**: under position 7 anything needing the excluded pairs reconstructs them, **and a reconstruction that agrees today is a second definition tomorrow.** **The `p` invariant is SPECIFIED** — a **DATA CHECK** on a set where four of six are code checks, run unprompted by both instances, and **Step 10 publishes `p`.** **The set-membership rule is a COVERAGE COUNT, not an invariant**, resolving the 7-against-6 split. **The 94-record denominator is REPORTED UNRECONCILED** — 6,065,704 against 6,065,610, **both 0 drops.** **D9 uses the STRICT key with the loose 75 alongside**: loose strips the year and merges **The Twilight Zone, The Traitors, Manhunt** — remakes, not split metadata — and **the loose count bounds how wrong strict could be, with the error running OPPOSITE to D9's own lower-bound caveat.** **`processed/` is surface 8** — `adopted_rule.json` carried revision-3 figures against the approved revision-6 rule and an instance worked around it, **second time it bit, and no control covered the first file an implementation reaches for.** Corrected; **254 files now scanned, up from 96**; data tables excluded with the count reported, per-arm working dirs allowlisted by name, and **`adopted_rule.json` NOT exemptible in code, checked before the allowlist.** | 2026-08-13 | Closed |
| [0075](0075-d3prime-d9-sideoutput-and-the-w-grid.md) | **D3′'s cleared shares, D9 half (b)'s side output, and the `W` arm grid.** **D3′ is 99.53% → 97.73%** on Step 8's right-censored populations, measured **independently and identically by both instances**; `0034`'s **95.98% → 91.34%** was on the amendment's **uncensored estimation sample** with no population stated. **Direction and shrinkage stand; the level does not** — and **`0068` §2a could fix the denominator and not the level**, which is why it needed a ruling. **Position 3's drop set is retained as a side output**: half (b) is measured on the rows position 3 removes and **no line said to keep them**, so an instance that misses it **emits ZERO — and a zero here reads as a data finding rather than a missing input.** **The `W` grid is 38 / 46 / 77 / 91 / 107 / 108 / 150 / 213, written down for the first time in any file** — Step 6's deliverables say `[37, 107]` and `[37.70, 107.71]`, **neither says 38**, and the grid travelled only as the **index of a reported series, which is a reading and not a specification.** **The most serious of the three: every Step 13 figure is indexed by the arm set, so two instances on different grids produce tables that cannot be diffed AT ALL — a failure of the dual implementation itself, not a wrong number inside it.** | 2026-08-13 | Closed |
| [0076](0076-p-is-a-code-check-and-two-real-ones.md) | **Red Team's first Step 8 review — HOLD.** **The `p` invariant is a CODE CHECK**, on both instances' proof: Started-and-left requires `\|A\| ≥ 1` so `m_H` exists, and set membership bounds the rank numerator in `[1, L2]`. **`0074` labelled it DATA CHECK without checking what the instances had labelled it** — the `0051` failure again. **It inverts the gate's own open item: the true figure was FIVE of six unfalsifiable with ZERO pure data checks**, not four of six, corrected in `0069`, `0070`, `0074`, `task-sheet.md` and both agent files. **Two genuine data checks added because the set had none** — no account dropped wholesale (703 pairs from 216 accounts is consistent with a pair-level AND an account-level implementation) and **no `access_denied` account read as empty, which fails in the direction of the result.** **D9's strict and loose keys are DEFINED** — they existed only inside one instance's code, which the other is forbidden to read, so the ruled key was undefined on every surface an isolated instance reads; a third key (arbitrary trailing digits, reducing `the-100` to `the`) produced **76 against 75**, a divergence missing from `0073` §3 and reconciled by `0074` §5. **Three propagation misses:** `CLAUDE.md` said eight surfaces once and **seven twice, in the operative instruction**; Step 1 §2.3's `action` requirement was live while `0073` cited it as the ground for marking §9; and **two of three edit batches this turn never persisted** — asserted mid-run, single write never reached, caught by grep and not by me. **Both arms rerun.** | 2026-08-13 | Closed |

**A note on authority.** Entries 0001–0004 and 0013–0033 are Human Lead decisions. **0005–0008 are agent-taken,
inside a Chained step, and are recorded retrospectively for ratification** — they shaped the
population every downstream number rests on, and a constant that shapes the population is a decision
whether or not it was treated as one at the time. They are listed here so the distinction between
"decided" and "defaulted into" stays visible.

## Gates

Five. Nothing downstream of a gate runs without written Human Lead approval at it.

- [x] **Step 1** outcome definition — approved 2026-08-10 ([0001](0001-step1-outcome-definition-gate.md)); **§7 reopened as an amendment and re-approved 2026-08-12** at `τ2 = ⟦T0⟧ + 199 days` for Continued ([0034](0034-step1-continued-boundary-amendment.md))
- [x] **Step 5** contamination exclusion rule — approved 2026-08-12 ([0021](0021-step5-contamination-gate.md)). *(An amendment at [0053](0053-0021-amended-two-windows.md) was **withdrawn the same day** by [0054](0054-revert-to-alt-broad-widen-the-floor.md); the ruling stands as approved.)*
- [x] **Step 6** window `W` — approved 2026-08-12 at **W = 108 days** ([0026](0026-step6-window-w-gate.md))
- [x] **Step 7** liveness rule — **APPROVED by the Human Lead, 2026-08-13** ([0064](0064-step7-gate-approved.md); record at `artifacts/step7-gate-approval.md`). **ALT-BROAD: not live iff no insertion after `τ1` AND NOT Continued**, silence anchored at `τ1` and only at `τ1`, channel window `(τ1, τ2)` open. **Exclusions 703 APPLY / 99 DERIV.** S&L bound **[9.6372%, 10.0405%]** APPLY, **[11.3015%, 11.4291%]** DERIV. **Fifteen Red Team reviews; the rule was uncontested from review 5 and cleared on measurement at review 15.** **Approval is UNCONDITIONAL with the §4 residual open and published.**
- [ ] Step 8 analysis table

## Open items carried forward

Recorded here so they are not lost between steps. Each is stated in full in the decision file
that surfaced it.

1. ~~**`pull_date` has no value.**~~ **CLOSED 2026-08-11** — set to `2026-08-11` as
   [0011](0011-pull-date-value.md). Right-censoring and D3/D8/D9 are unblocked.
2. **Step 1 open questions 1 and 3** remain open. The drafted boundary stands until decided.
   ([0001](0001-step1-outcome-definition-gate.md))
3. **The gap hypothesis is untested** — whether Trakt omits a gapped episode number or lists a
   placeholder. Section 3 is not a claim that gaps are handled. Not yet assigned to a step.
   ([0001](0001-step1-outcome-definition-gate.md))
4. ~~**Provenance gap:** the 28 percent inflation figure and the six-week overlap cited in Step 1
   §2.1 and §5 are not reproducible from the repo.~~ **CLOSED 2026-08-10** — reproducible at zero
   live calls via `src/step0_history_probe.py`, with a run record and a public write-up. Both
   figures reproduce (28.125 percent, 5.90 weeks). It also produced a post-approval addendum to
   Step 1 §5. ([0001](0001-step1-outcome-definition-gate.md))
5. **An unobserved premise inside an approved rule:** Step 1 §2.1 asserts `episode.ids.trakt` may
   disagree with `(season, number)`; the probe shows 96 IDs, 96 pairs, zero disagreements. Not
   contradicted, unobserved — but it is the mechanism **D9's split signature** depends on.
   ([0001](0001-step1-outcome-definition-gate.md))
6. **D2 cannot size the failure the §5 addendum names.** D2 runs on the operative first-pass
   clock, where the artifact does not appear. **Expect zero, and zero is not evidence of
   rarity.** Sizing it requires D2 inside the Step 13 last-observed arm.
   ([0001](0001-step1-outcome-definition-gate.md))
7. **The `L2 = 1` / cadence-classification ordering is written nowhere.** Classification is
   available from Step 2; the `L2 = 1` exclusion happens at Step 8. At `L2 = 1` the weekly span
   is 0, which falls in bucket C2 — harmless only because those shows are excluded, and the
   order that makes it harmless is not recorded. **Does not arise on the current frame
   (2026-08-12): `min(L2) = 2` and zero in-frame shows have `L2 = 1`,** so no show is
   misclassified by the ordering today. The ordering is still unwritten and the frame will change
   if the pull resumes, so this stays open. ([0019](0019-pool-completers-recomputed.md))
8. **The gap hypothesis still has no owning step** — see item 3. Visibility is not ownership.
9. **The 403 rule's live behaviour is still unobserved — Step 3 did not exercise it.** 5,300 calls
   produced zero 403s and zero 429s, so both 403 branches and the 429 path remain untested against
   the live API. **Step 4 is now the first exercise.** What Step 3 *did* exercise, nine times and
   successfully, is the retry-with-backoff branch (16 HTTP 5xx, 1 transport error, all recovered).
   One of the three failure paths is live-tested; two are not. ([0004](0004-403-handling.md))
10. **The pool is biased toward heavy, currently-active trackers**, direction **downward on the
    never-started share**, compounding with the liveness-exclusion bias rather than cancelling it.
    Step 11 as written cannot detect it: both channels select on public-facing activity, so
    agreement between them is not evidence of unbiasedness. Either Step 11's brief gains an
    activity-stratified diagnostic — computable from `raw/step3/` at zero further live calls — or
    Step 14 states the limitation. ([0005](0005-step3-stopping-rule.md), `artifacts/step3-user-discovery.md` §4)
11. **Step 4 costs ~210,500 calls and ~23.4 hours** of pure throttled time at the current pool,
    roughly 2.4× an earlier sampled estimate that inherited a `total_plays` bug.
    **Partly addressed:** [0009](0009-step4-pull-order.md) makes an early stop survivable and
    [0010](0010-step4-tail-cap.md) trims 1.7 hours, but the run is still not expected to finish the
    pool in one window. Whether to sample the pool down remains unsettled.
    ([0006](0006-step3-crawl-constants.md))
12. ~~**`reciprocal_pairs: 1353` is a counting bug.**~~ **CLOSED 2026-08-11.** Fixed in
    `src/step3_backfill.py` and regenerated: `artifacts/step3-yield-curve.json` now reports
    **1,172**, matching an independent recount from `raw/step3/edges.jsonl`.
    `distinct_directed_pairs: 7103` was always correct. ([0007](0007-step3-channel-cost-trade.md))
13. ~~**`MIN_EPISODES_USABLE = 10`'s warrant is unverified.**~~ **CLOSED 2026-08-12 — checked against
    the frame and the retained screen records, and ACCEPTED by the Human Lead.** Verified facts, in
    the order they were asked for:
    - **What it counted.** `src/step3_user_discovery.py:953–954`, `ep_watched =
      int(episodes.get("watched") or 0); usable = ep_watched >= MIN_EPISODES_USABLE` — the
      `episodes.watched` field of `GET /users/:id/stats`, an **account-wide count of distinct
      episodes across all shows**. Not play records: the same block reads `episodes.plays`
      separately. Across all 4,319 cached stats bodies `watched < plays` in 3,523 and
      `watched == plays` in 796, **never greater**. It is **not per-show** — ten episodes across ten
      shows passes, nine inside one show fails.
    - **What it removed.** **232 accounts, and nothing else removed any.** Over 4,320 screened:
      `ok` 4,088, `below_episode_floor` **232**, `access_denied` 0, `unavailable_*` 0, `status_*` 0.
      Corroborated by `logs/step3_run.json` and `artifacts/step3-user-discovery.md` §1.
    - **What those accounts held. 210 of the 232 have `episodes_watched = 0`.** Only 22 have any
      episodes at all, the maximum being 6 (counts: 0→210, 1→5, 2→3, 3→5, 4→4, 5→1, 6→4).
    - **The warrant is still not literally true**, and that is what is being accepted rather than
      denied: `min(L1) = 1` over the frame and 152 in-frame shows have `L1 ≤ 6`, so a 6-episode
      account is not arithmetically barred from having completed an in-frame S1. **The exposure is
      at most 22 accounts, 0.5% of the 4,320 screened**, not the 232 the floor rejected.
    - **The removed accounts are fully recoverable** — slug and complete screen record for all 232
      in `raw/step3/user_pool.jsonl` and `raw/step3/state.json`, with their stats bodies already
      cached under `raw/users/*/stats/`. Recovering them costs **0 live calls**; a full history pull
      of all 232 would cost **296 pages** (30 for just the 22 with any episodes), and the forecast is
      exact at that size — observed users with a 1-page forecast read exactly 1.00 pages, 2-page
      exactly 2.00.
    - **Not recoverable:** what the crawl would have found had those accounts remained in the
      frontier as expansion sources. That path was not taken and is not reconstructible from disk.
    ([0006](0006-step3-crawl-constants.md), `artifacts/step2-frame-ledger-and-distributions.md` §3.2)
14. ~~**The Step 3 seed source has no decision entry.**~~ **CLOSED 2026-08-11** — recorded as
    [0008](0008-step3-seed-source.md).
15. ~~**A rule inside the approved Step 1 gate was amended without Red Team review.**~~ **CLOSED
    2026-08-12** — [0012](0012-sweep-completeness-rule.md) went to Red Team, which returned **HOLD**,
    and the Human Lead **upheld the rule on cascade cost, not on merit**, as
    [0023](0023-step4-completeness-rule-upheld.md). Three findings travel to Step 14 instead: the
    rule validates itself against itself (leg 1 gates on `page_count`, which is
    `ceil(item_count/250)`); the discard is **not outcome-neutral** (+1.27 pts, CI [0.87, 1.66],
    pushing never-started **up** and compounding with the seeding and liveness biases, pooled effect
    0.13 pts); and Red Team's final-page shape test would discriminate exactly, at ~2,800 calls and
    no re-pull, and was declined on cost. Also recorded there: the 2% was set on a 20-user pilot
    whose own maximum residual was **11.7%**, not the 0.86% 0012 quotes; any tolerance from ~1.5% to
    11.7% gave the identical pilot partition and the most aggressive end was taken; and **31 of the
    287 discards were under-count users** removed for returning more data than advertised, which
    0012's own text calls benign. **If the pull ever resumes the cascade argument weakens and the
    shape test should be reconsidered rather than inherited as settled.**
    ([0012](0012-sweep-completeness-rule.md), [0023](0023-step4-completeness-rule-upheld.md))
16. **`step3_backfill.py --out-dir raw/step3` zeroes the call ledger in `state.json`**, because the
    offline replay spends no live calls. **This has happened twice** and been restored from
    `logs/step3_run.json` both times; a `ledger_note` in the file records it. Either restore after
    every regeneration or use the script's default out-dir, which does not touch that file. The
    ledger is the only record of Step 3's spend against the API budget and it is gitignored.
17. ~~**The Step 2 structural thresholds are deferred, not skipped.**~~ **CLOSED 2026-08-12** — set
    as [0020](0020-step2-structural-thresholds.md): no minimum season size, max 26 episodes on
    either season, max 1,095-day gap. Frame 1,226 → **1,138 shows, 220,107 pairs**. A headline is
    **no longer provisional on this ground**; it remains provisional on the four unapproved gates
    (Steps 5, 6, 7, 8). **New consequence carried forward:** the size cap removed 44 C4 shows of 51,
    so it is partly a cadence threshold and **a C4 result is computed on a population stripped of
    its longest-running titles** — C4 being where abandonment is most likely exposure-driven.
    ([0014](0014-no-content-filters-structural-fields.md), [0020](0020-step2-structural-thresholds.md))
18. **Platform fragmentation — problem (a) CLOSED 2026-08-12, problem (b) still open and relocated.**
    ~~(a) it is not established whether Trakt exposes a **per-season** network.~~ **Measured: it does,
    and it is empty — 0.71% populated across 6,645 season objects, one show in 2,094 with two
    distinct values.** Dropped as a field per 0014's own resolution rule, recorded as
    [0016](0016-per-season-network-dropped.md). **Platform fragmentation is not a variable in this
    study**; no result may control for it, stratify on it, or rule it out.
    **(b) survives, and now attaches to a different field.** The show-level `network` pulled on
    2026-08-12 is 100% populated with 150 distinct values, and it records **today's** network — a
    title that moved services between seasons shows only its current one. **`show_network` must not
    be used as a release-time availability measure.** Retained as a descriptive field only.
    ([0014](0014-no-content-filters-structural-fields.md), [0016](0016-per-season-network-dropped.md))
19. **The Step 2 frame is built on a stopped pull, and every frame-derived boundary moves if it
    resumes.** The candidate set rests on 2,549 users, 62.9% of plan. The stop is proportional across
    all ten strata (decided share 69.6–70.1%), but the *analysed* cohort is proportional only to
    within 6.1 points, because the [0012](0012-sweep-completeness-rule.md) discard rate varies by
    stratum (complete share 59.3% in bin 5 to 65.4% in bin 7). Consequences that are not yet
    decisions: the ≥50 candidate rule was applied to **proxy** counts and has not been re-applied to
    the recomputed ones ([0019](0019-pool-completers-recomputed.md)); **size quintile boundaries are
    valid only against the frame they were cut on** ([0018](0018-size-quintile-base.md)); and the
    frame is systematically older than the catalogue, 66.5% of it with an S2 finale before 2020
    ([0015](0015-step2-unaired-s2-exclusion.md)).

20. **Four Step 5 limitations travel to Step 14**, as stated limitations and not pending decisions
    ([0021](0021-step5-contamination-gate.md)). **(a)** The **4,988 partly-air-date pairs** (2.27%)
    carry the same deterministic guarantee that excluded the 16,665 — a single stamped S2 record
    forces `|A| ≥ 1` — and cannot be resolved until `W` exists; a `W`-dependent test was rejected
    because it would make the analysis population a function of `W` and **corrupt the
    dual-implementation control**. **(b)** The **floor claim is guaranteed for 8,372 pairs and
    assumed for 42,019**, because `backfilled` means claimed ≪ *insert*, not claimed < *true*;
    Step 14 publishes the direction and must publish the qualifier with it. **(c)** The **flip bound
    is 0 to 44,458** at `W = 60` and rules out only ~5% of candidates — no point estimate exists.
    **(d)** The exclusion's **population change is net up by 15,123 pairs** while the estimator bias
    runs down; the two are different kinds of quantity and **must not be netted**.
21. **The play-`id` insert-time calibration is now a required Step 7 input**, not a Step 5
    diagnostic, under the standing ruling that liveness runs on record insertion time rather than
    claimed watch date ([0021](0021-step5-contamination-gate.md)). Step 7 cannot be specified
    without it.
22. **An uncommitted figure is also an unverified one.** Figures printed in a gate artifact with no
    committed code behind them recurred **three times** inside Step 5, twice inside the sections
    written to close the defect. When the last one was finally committed it proved **wrong as well
    as uncited** — a maximum of 198, not the 164 printed, the 164 having come from the first 4,000
    of 155,626 groups in an exploratory shell. Worth a standing check at every remaining gate.
    ([0021](0021-step5-contamination-gate.md))

23. **When a gate ruling changes what a downstream step computes, propagate it to `task-sheet.md` at
    the time of the ruling, not at the time the step launches.** Step 5's two standing rulings lived
    only in `decisions/0021` and the Step 5 artifact for a day; the Step 6 and Step 7 specs the two
    isolated instances read said nothing about them. **The dual-implementation diff cannot catch a
    spec omission** — both instances read the same silence and agree on the wrong answer. Fixed as
    [0022](0022-step5-rulings-written-into-task-sheet.md). Three gates remain and each will produce
    rulings with downstream reach. ([0022](0022-step5-rulings-written-into-task-sheet.md))

24. **`decisions/0003` D14's warrant is factually wrong, and 95 negative lags have no explanation.**
    D14 and Step 1 §9 both state that every C1 lag is non-negative by construction. **689 are
    negative (2.74% of C1)**, found independently by both Step 6 instances with identical counts.
    459 bind on the S1-completion term — `max()` can select it on a C1 show, so the guarantee only
    ever held for the finale term — and 230 bind on the finale, which should be impossible under C1.
    Of those, 135 are the known one-day UTC finale skew and **95 are beyond it, out to −495 days**,
    an unexplained timestamp or metadata defect Step 8 should expect to meet again. Worth ≤6 days of
    `W`, so not load-bearing for the number, but the warrant is false either way.
    ([0024](0024-w-is-the-90th-percentile.md))
25. **Dual-implementation instances need distinct output namespaces.** Step 6 run 1's two instances
    were given byte-identical prompts — correct for spec neutrality — but identical default output
    paths, and they collided: one instance's script was overwritten mid-run. **The namespace is not
    part of the task description and separating it does not weaken the diff.** Applies to Steps 6, 7
    and 9 (`data-scientist` / `-b`) and Step 8 (`analytics-engineer` / `-b`).
    ([0024](0024-w-is-the-90th-percentile.md))

26. **Three Step 6 findings share one shape: a convention doing work the data does not do, left
    unstated in the spec.** "The percentile where the curve flattens" (undefined; the C1 density is
    close to scale-free past day 7, so there was no feature to read — two instances 61 days apart);
    the 90th percentile itself (imported from attribution-window practice, not selected by the data);
    and the unit of the lag with the rounding of the percentile (one day). **Each was caught only
    because two isolated instances ran the same words and produced different numbers.** None would
    have been visible in a single run. Expect the same shape at Steps 7 and 8.
    ([0024](0024-w-is-the-90th-percentile.md), [0025](0025-lag-unit-and-ceiling.md))
27. **Step 6 and Step 8 will not share a row set.** Four pairs in the 128,099 have a first S2 record
    at or after `τ_pull`, which Step 1 D11 says must be discarded from every computation, while
    Step 5 built the sample without that filter. Both Step 6 instances found them and both retained
    them, correctly, because the spec directs taking the population from the Step 5 artifact. **None
    is in C1, so `W` is unaffected**, but Step 8 classifies on the 201,900 under the frozen cutoff.
    Unresolved. ([0026](0026-step6-window-w-gate.md))
28. ~~**Step 13's mandated arms top out at the adopted `W`.**~~ **CLOSED 2026-08-12** — arms added
    at **150 and 213 days** as [0027](0027-step13-w-arms-above-the-adopted-value.md), so the tested
    span is effectively **38 to 213** with the adopted `W = 108` inside it rather than at its
    ceiling. 213 is the C1 p90 among pairs with ≥8 years of exposure, an **upper bound** on the
    censoring correction rather than a rival estimate, since exposure and cohort are not separable.
    **Watch the denominators:** right-censoring retains a pair only if
    `⟦T0⟧ + (max(W, 91) + H) × 24h ≤ τ_pull`, so an arm at 213 discards materially more pairs than
    one at 108 and the headline moves for two reasons at once — the per-arm retained-row count
    already required by Step 13 is what keeps them separable.
    ([0026](0026-step6-window-w-gate.md), [0027](0027-step13-w-arms-above-the-adopted-value.md))

29. **Step 14's limitation list is a floor, not a ceiling.** [0028](0028-step14-carries-every-routed-limitation.md)
    brought it current at seven bias statements and eight non-bias limitations. **Steps 7, 8 and 9
    through 13 will route more**, and each should be written into `task-sheet.md` Step 14 **at the
    time of its ruling** rather than accumulating for a sweep later — item 23's rule, applied to a
    step whose failure mode is plain omission rather than silent divergence.
    ([0028](0028-step14-carries-every-routed-limitation.md))

30. ~~**The Step 7 liveness percentile is proposed at the 99th and not ruled.**~~ **CLOSED** — ruled at
    the 99th by [0036](0036-step7-threshold-and-shape.md), then **the whole threshold was DELETED** by
    [0042](0042-step7-liveness-gate-parameter-free.md) because the headline could not distinguish 787
    from 2,200 days. **There is no percentile.**
31. ~~**The liveness rule's shape is unsettled, and no percentile fixes it.**~~ **CLOSED** — the shape
    was ruled by [0036](0036-step7-threshold-and-shape.md) §2 and the rule has since been replaced
    three times, ending at **ALT-BROAD** ([0048](0048-liveness-rule-alt-broad-adopted.md)). The item's
    own observation — that no percentile fixes a shape problem — is what led to the threshold's
    deletion.

32. **Read each remaining spec for undefined conventions BEFORE its step launches.** Three
    consecutive gates needed one written in first: "flattens" and the lag unit at Step 6, then the
    gap unit, the threshold criterion and the filter order at Steps 7 and 8. Two were caught only
    because a dual run burned a cycle. **Steps 8 and 9–13 should be read for the same defect before
    each launches, not after.** ([0026](0026-step6-window-w-gate.md), [0029](0029-step7-threshold-rule-and-w-propagation.md))

33. **The right-censoring margin is 24 days and censoring is cohort-asymmetric.** The frame caps the
    S2 finale at 2025-12-31 and censoring admits `T0` to 2026-01-24 at `W = 108`. Zero shows are
    lost, but `W` is ±18 days show-clustered, so **the slack is smaller than the uncertainty in the
    number consuming it**. The loss falls on the uncapped `S1_completion_date` term and is
    cohort-asymmetric: at `W = 213` the 2023–2025 cohort loses **10.3%** of pairs against **2.7%**
    pre-2020, and survivors from recent titles skew to early adopters — the users likeliest to
    continue. **The arm added to test the censoring bias is the most censored arm.** Carried to
    Step 14. ([0030](0030-frame-field-corrections.md), [0027](0027-step13-w-arms-above-the-adopted-value.md))
34. **`show_rating` is a contaminated predictor and any cut using it must say so.** It is measured in
    2026, after S2, so a show whose S2 disappointed may carry a depressed rating **because of the
    outcome being studied**. `show_votes` is far less contaminated and is external to the pull. The
    field was added because the alternative was **no reception stratifier at all**, and a disclosed
    confound beats an absent variable. ([0030](0030-frame-field-corrections.md))
35. **The frame is a large-title frame and no result generalises to small titles.** The ≥50 floor
    admits 2.6% of the shows the pool has evidence for. A slate's hard renewal calls cluster in the
    **marginal** performer, not the top 2.6%, so **Step 15's decision rule must not be phrased as
    though it covers them**. The floor is also a higher bar for recent titles, making the 2023–2025
    cohort differently selected rather than merely smaller.
    ([0031](0031-the-50-completer-floor.md))

36. **Two Step 4 record defects are carried as Step 14 limitations rather than fixed**, because both
    are provenance defects and neither moves a result. **(a)** The live-call ledger **under-counts by
    246** — a run killed mid-sweep never writes its row while its calls have already been sent, and
    the three documented guards all address replay *zeroing* rather than this. **`api_requests.ndjson`
    is the authoritative spend figure.** **(b)** `step4_failures.ndjson` reads **301 against 287**
    terminal discards, carrying 14 duplicate rows from an offline replay, because `OfflineGuard`
    does not partition the failure log or the API log by `--state-dir`. **Read failure counts from
    the ledger.** ([0032](0032-step4-deliverable-regeneration.md))
37. **The resume is unpriced in the file that would be used to price it.** `step4_progress.json`
    still reports `projected_hours_remaining: 4.28` against a measured **~7.8 hours and ~70,000
    calls**. The exact figure is one summation over `pull_order.jsonl` at zero API calls and **has
    not been computed.** Resume hazards also stand unaddressed: an exclude file with no expiry, disk
    unmeasured anywhere, and a 27-hour Sabbath window a 7.8-hour run does not fit before a
    Friday-evening start. ([0032](0032-step4-deliverable-regeneration.md))

38. **The sweep-completeness discard has an unidentified selection mechanism.** Per-bin discard rates
    run 6.3% to 15.5% against a pooled 10.1%, with **bin 5 at roughly +3 SD**. **Sweep length is ruled
    out** — no monotone trend across bins spanning 1 to 292 forecast pages — which is the useful half
    of the finding and is why ~10% remains safe planning for any resume. What drives it is not
    established. **It compounds with the discard already being outcome-non-neutral**
    ([0023](0023-step4-completeness-rule-upheld.md), +1.27 pts): a mechanism that is both
    outcome-correlated and unexplained cannot be argued harmless. Characterising it would mean
    re-reading the 287 discards' raw pages — the same class of work `0023` declined on cascade cost.
    Carried to Step 14. ([0033](0033-step8-per-air-period-censoring-counts.md))
39. **Both partner reviews are now fully disposed**, Product findings 1–5 and its Step 15 warning,
    Engineering findings A–G. The record is `artifacts/partner-reviews-steps-2-and-4.md`. **The Step
    15 warning is a live commitment, not a closed item**: the Product reviewer has stated it will ask
    whether the decision rule's action is stated at a confidence the 2023–2025 evidence — 168 shows,
    release-strategy cells of 26 to 59 — actually supports.
    ([0033](0033-step8-per-air-period-censoring-counts.md))

40. **The Step 1 §7 amendment has no stated ground for its anchor choice.** There is no argument
    anywhere in the approved text for preferring `τ2 = ⟦T0⟧ + (W + H)` to **first-S2-watch + `H`**.
    Four were attempted across revisions 6, 7, 8 and 12 and **all four failed review** — exogeneity
    (false: `T0`'s `S1_completion_date` term binds on **52.7%** of pairs), temporal position (does not
    bind: D10 already imposes the clearance), arm comparability (false: no diagnostic window changes
    length), and choice-at-a-price (false and backwards: `τ2` gives every starter **more** than 91
    days, and item 9's set **grows** under the start anchor). **§2 of the amendment carries the marginal-lag
    distribution — not Step 2, which is the frame ledger and has none — and it is the start-anchored
    rule's own distribution**, so the study computes the alternative's evidence and
    uses it to grade the adopted rule. **Not to be repaired by a fifth attempt without new evidence.**
    ([0034](0034-step1-continued-boundary-amendment.md), `artifacts/step1-amendment-continued-boundary.md` §21)
41. **D11 is applied to the amendment's record set only, not to the Step 5 pair table or the Step 6
    lag script.** The 128,099 can therefore contain pairs whose only S2 evidence is post-cutoff, and
    `T0` may be fixed by a post-cutoff S1 record. **The amendment's §2 marginal p90 of 100.39 is pre-D11** — §2 of
    `artifacts/step1-amendment-continued-boundary.md`, produced by `src/step6_completion_lag.py`, not
    Step 2 of the task sheet.**
    Extending the filter would move the **Step 5 waterfall** and the distribution **`W = 108` was
    derived from**, both inside approved gates, so the limit is **disclosed rather than chased**.
    Bounded: 77 records / 28 pairs in-sample, **1,734 of 27,656,434** account-wide, and the 4 pairs
    whose first S2 record is post-cutoff are **not in `W`'s C1 derivation population**, so `W` cannot
    move. ([0034](0034-step1-continued-boundary-amendment.md))
42. **`first_s2_lag_days` is a backfill measure and was misread as a start-time filter for six
    revisions of the amendment draft.** It is `tau − ts` — a record's estimated insertion instant minus
    its claimed `watched_at`. **Step 5 named it correctly throughout**, so the approved Step 5 gate is
    untouched, but two ledger entries were argued from the misreading before it was caught. The floors
    now rest on the contamination-exclusion channel — **50,066** pairs, not 73,801, since the 23,735
    dropped at the waterfall's second step have `A_H = ∅` and can enter no numerator.
    ([0034](0034-step1-continued-boundary-amendment.md))

43. **Q1 remains open but its warrant cites a diagnostic `0034` replaced.** §10.1's open question 1 —
    whether the Continued conjuncts are the right ones — rests in part on *"D3 covers the strict rule's
    real weakness"* and is triggered by *"if D3 returns a high resumption share."* **`0034` replaced D3
    with D3′** because that quantity became the operator itself. **Q1 is genuinely still open** — it
    asks about the **conjuncts** while the amendment moved the **instant** — but its stated warrant has
    a dangling term and should be restated against D3′ before it is answered.
    ([0034](0034-step1-continued-boundary-amendment.md), `artifacts/step1-outcome-definition.md` §10.1)
44. **The Step 6 headers' "unaffected by the rendering" clause overreaches.** `0029` closed the
    rendering question correctly, but both Step 6 artifacts publish a **Step 13 minimum range** —
    `[37, 107]` and `[37.70, 107.71]` — which *is* a rendered figure and does move to `[38, 108]` under
    `0025`'s ceiling rule. **Practically moot**, since `0027`'s arms run to 213 and therefore cover it,
    but the clause claims more than it should. ([0035](0035-agent-definitions-and-propagation-cleanup.md))

45. **The 38,696 "no instant at or before `τ1`" pairs are a clock mismatch and route to Step 14.**
    `T0` is built from claimed `watched_at` while liveness runs on **insertion time** (`0021`), and for
    old shows the two clocks are far apart. The **median pair's `τ1` falls 1,578 days before the
    account's first-ever insertion instant**, and **8,037** have `τ1` before the calibration curve
    starts. **These are not absent users** — they are pairs whose window closed before the account
    existed on the insertion clock. 19.17% of the 201,900 population, falling to **1,038** in the
    128,099 clean sample, so dominated by early or corrupt `T0` **but not empty there**. Recorded, not
    repaired: repairing it means reconciling two clocks `0021` deliberately separated.
    ([0037](0037-step7-reference-distribution-and-gap-unit.md))

46. **A ruling lands in a decision entry and not in the file the agents read — TEN times now.**
    `0035` (agent definitions never amended; `0034`'s Step 10 requirements in no spec), `0036` (applied
    to a bullet body, not its header), `0039` (corrected `0038` §5 and not `task-sheet.md` line 248,
    plus three superseded figures), `0040` (corrected four task-sheet figures and **missed line 266 —
    the one that sets the threshold**). **Every instance was caught downstream** — by an agent or by
    Red Team — **rather than at the point of the edit.** The pattern is not carelessness about a
    particular file: **the decision entry feels like the place a ruling lands, and it is not.**
    `CLAUDE.md` sends agents to their definition file first and `task-sheet.md` second; **neither is
    `decisions/`.** **Count corrected from five to ten, 2026-08-13 ([0051](0051-v1-v7-and-housekeeping.md)); the four standing controls are item 46's five-file surface, `0046` §0's population rule, `0047` §3's endpoint/estimand rule, and `0049` §6's agent-launch snapshot practice. SHARPENED 2026-08-13 after a sixth instance ([0044](0044-liveness-w-coupling-and-propagation.md) §3.1): the propagation surface is FIVE FILES — `task-sheet.md` and the four pipeline agent definitions — and an entry that changes a rule must STATE which of the five it touched and which it deliberately did not. `0042` named none, which is how the rule reached neither `analytics-engineer` file.** A ruling that names no files has not been propagated, whatever the entry says. **EXTENDED TWICE 2026-08-13. First ([0046](0046-liveness-rule-alt-adopted.md) §0) after three consecutive entries each corrected the previous one and each committed the same error — a figure measured on one population quoted as another: EVERY FIGURE IN AN ENTRY STATES WHICH POPULATION PRODUCED IT, AT THE POINT OF USE. An entry that cites a number without its population is not propagated.** **Then ([0047](0047-step7-alt-rerun-corrections.md) §3), after a THIRD consecutive bound whose endpoint did not cover the case the filter exists for: AN INTERVAL ENDPOINT STATES THE POPULATION IT IS COMPUTED ON AND THE ESTIMAND IT BOUNDS, AND THEY MUST BE THE SAME POPULATION.** The recurring cause is publishing an endpoint computed on the *filtered* population against an estimand defined on the *unfiltered* one.
    ([0041](0041-step7-extended-set-and-sensitivity-precondition.md) §5)

47. **Step 7's outcome shares are provisional in POPULATION, not only in status.** The sensitivity
    test ran on line 4 less D10 — **147,370** — while Step 8 applies liveness at position 6 to the
    **analysis population less D10, 196,654**, a strict superset whose extra lines carry **contaminated
    `T0`**, and **both `τ1` and `τ2` are built from `T0`**. **The absolute shares will move at Step 8;
    the flatness finding will not**, because the exclusion sets are 0.51–0.87% of the population and
    that survives enlarging the denominator. **The levels in `0042` §2 are not results and must not be
    quoted as any.** ([0042](0042-step7-liveness-gate-parameter-free.md) §5)
