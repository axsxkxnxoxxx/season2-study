# Decision 0117 — E8 and E14 reached zero of surfaces 2 and 3; the TWO-field key was live there in the present tense

| | |
| :--- | :--- |
| **Decision** | **No new ruling.** ***`0114`'s E8 and E14 reached surfaces 1, 4–5 and 7 — and ZERO of surfaces 2 and 3, the files Steps 9–13's writers read.*** **`channel_classes` appeared 0 times in either; the revision appeared once with no instruction to READ it.** ***And the TWO-field arm key stood there in the PRESENT TENSE, fourteen lines below its own replacement.*** **Fixed. Step 13b's spec said seven, then eight; the anchor requires NINE.** |
| **Recorded by** | Analytics Engineer |
| **Date** | 2026-08-19 |
| **Occasioned by** | `reviewer-engineering`'s fifth Step 8b review: **not clear**, on F1 and F2 |
| **Amends** | `0114`'s propagation; `0116`'s surface report; `task-sheet.md`'s Step 13b section |
| **Status** | Open. **Step 9's last blocker is levels-vs-movements, and arm A's report has changed what it is.** |

---

## 1. E7's shape, third occurrence — and I made it twice in three entries

***`reviewer-engineering`: "the same class as my last verdict: E7's shape, third occurrence."***

**`0114` propagated E8, E13 and E14 to surfaces 1, 4–5 and 7.** **Measured on disk before fixing:**

| | |
| :--- | :--- |
| **`channel_classes` in either `data-scientist` file** | ***0 occurrences*** |
| **`adopted_rule_revision`** | **1** — the key statement only, **with no instruction that it must be READ rather than typed**, no path to `processed/step5/adopted_rule.json`, and no mention of the reader |

***The seven arm files E8 and E14 govern are written by exactly these two agents.*** **They would have got the absence idiom right only by imitating the placeholder — and would have TYPED the revision, because nothing told them not to and the validator accepts a typed one silently.**

## 2. The two-field key, present tense, fourteen lines below its replacement

`.claude/agents/data-scientist{,-b}.md:169-170`, byte-identical, **under the heading *"What the schema
already accounts for, so you do not rebuild it"***:

> *"one entry per arm, keyed on `(W_days, clock_origin)` — NOT `W` alone"*

***That is the TWO-field key — superseded twice — stated as current, with the correct four-field
statement at line 155.*** **`0116` reported surfaces 1, 4–5 and 7 and never checked 2 and 3 for this
ruling.**

***Third consecutive entry in which a stale arm key was invisible to `check_surfaces.py`***, and the
reason has not changed: **an arm key is a STRUCTURAL CLAIM — not a number, not a registered withdrawn
phrase.** ***All three were found by a reading agent.***

**And this is `0113` §2 for the second time in five entries** — the entry that made the honest
propagation report mandatory, followed by two propagations that did not give one.

## 3. Step 13b's spec counts wrong, in three places, in one section

**`task-sheet.md:1176-1177` says *"INPUTS FIXED AT SEVEN"*. Line 1199 corrects itself to *"an eighth
source"*. The anchor requires NINE.** ***Step 8's artifact appears nowhere in the Step 13b section*** —
**`0114` E8 reached the Step 8b section of that file and not the consumer's.**

**So a Human Lead building the merge from the Step 13b spec would declare eight and fail `S30` leg (f) on
the first run.** **Corrected, along with the merge-only block list, which named two and is now four.**

## 4. Carried — and one is a rule this chain retired one version ago

***F3: `read_not_typed` is an unchecked `const: true`.*** **`S37` asserts the registry exists and that
entries agree with it. It never opens `adopted_rule.json`, never compares the revision to disk, never
recomputes the SHA.** ***`reviewer-engineering`: "which is precisely `diff_precedes_merge: true`,
retired at v1.3.0 on my own finding as 'not a fact the file records, a sentence the schema requires the
file to contain.' It has been reintroduced one version later"*** — **and unlike that one, the checkable
fact is on disk and the reader already exists.** **One function call.**

**F7 — a FILLED block at a clock origin where the spec says nothing produces one is a NOTE, not a
failure.** ***`S36`'s own asymmetry re-created one level in: absences policed, presence not.*** **E13
did not rule this direction. Human Lead ruling, then a validator change.**

**F5, F6, F9** — the schema's `sources_merged` description enumerates eight and stops (**E10's class,
surface 6**); a merged document can silently drop `discovery_channel_overlap` because
`_expected_merge_sources()` **holds the block tuple and discards it**; and the key is stated three ways
inside the control and the generator — including ***`S36`'s own failure text, which tells its reader the
key is three fields.***

**The reviewer confirms the four-field key IS stated identically at every four-field site** — same
names, same order, no paraphrase — **and that the five marked sites are correctly marked.** **One
citation defect: `S2`'s failure message attributes the four-field key to `0111` E2, which made it
three.**

## 5. Scope

- **Surfaces reached: 1** (Step 13b's source count and merge-only blocks) and **2–3** (E8, E14, and the
  two-field key). **`src/` and surface 6 are the arm's.**
- **Zero API calls. Nothing launched. Step 9 NOT begun.**
