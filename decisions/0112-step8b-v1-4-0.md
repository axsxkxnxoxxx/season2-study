# Decision 0112 — Step 8b v1.4.0: three rulings implemented, three defects reproduced and fixed

| | |
| :--- | :--- |
| **Decision** | **No new ruling. Step 8b v1.4.0.** **`0111`'s three rulings are implemented; Q1, E3b and E4 were each REPRODUCED before being fixed and each fix demonstrated against exactly the reproduced file; the M1 residual is corrected in three places from ONE expression.** ***This entry was committed under its label and not written until `0113` — the FOURTH occurrence.*** |
| **Recorded by** | Analytics Engineer, on arm `a`'s Step 8b rerun |
| **Date** | 2026-08-18 *(entry written 2026-08-19 — see `0113`)* |
| **Verified by** | Three placeholders at **exit 0**; **36 checks, 0 failures, 0 VACUOUS**; **69 selftest cases all with force** |
| **Status** | Superseded in part by `reviewer-engineering`'s fourth review. **See `0113`.** |

---

## 1. The three fixes, each reproduced first

**Q1 — the duplication in the arm's own placeholder.** **Reproduced**: the shipped sole file carried
`action_type_counts` filled while its own table said `published_by_step: step9`,
`may_first_writer_fill: false`, **and no check failed.** ***And the arm found the root cause alongside:
the OWNERSHIP ROW ITSELF WAS WRONG*** — `0111` E1 names `action_type_counts` one of **Step 13's**
outputs. **The duplication and a mislabelled owner were one defect.** New check **S36** polices
**presence**, so both directions are covered.

**E3b — `S30`'s missing direction.** ***Reproduced TWO orphaned inputs, not one***: `(step12, sole)` and
`(step10, sole)`, the latter carrying **no `merged_from` at all** because its arm is `sole` by
construction. **The selftest case is an OMISSION, no forgery.**

**E4 — `S22` reading the table out of the file under test.** **Reproduced**: one string edited in the
arm file's own `block_ownership`, `retained_by_air_period` emptied, **validated clean with S22's sites
unchanged at 19.** **`BLOCK_PUBLISHER` and `TOP_LEVEL_PUBLISHER` now live in the validator**, carrying
`S31`'s reasoning verbatim.

## 2. The three rulings

**E2** — the key is **`(W_days, clock_origin, producing_step)`**, asserted by S2, with the merged
document holding **8 entries** including five steps at `W = 108` finale-anchored. **Nothing restricts
which step may occupy a shared `W`.**

**E1** — the six take per-arm nesting via **one factory**, 6 definitions and 32 reference sites.
***The nesting is UNCONDITIONAL***: a block another step publishes is **omitted**, not wrapped in an
absence branch — **which keeps `by_producing_arm` outside any `oneOf`**, per `0109` §4's constraint.

**E6** — `arm_files_merged` → `sources_merged`, **renamed so a stale writer fails loudly**; **8
sources**, the eighth `non_arm_file / human_lead / arm: null / fills_blocks: ["limitations"]`.

## 3. Verified as instructed

***`S35`'s root-side scan WAS dead code.*** The old call found **0** sites on a schema mutated to reach
a container through a root-side `oneOf`; the fixed call catches it, and **that case is now a selftest
schema mutation.** **`by_producing_arm` remains outside any `oneOf`.**

## 4. The M1 residual, corrected

**Published in `known_limits_of_this_schema`, in the validator's runtime note, and in the
merge-provenance spec choice** — ***all three from ONE expression***, which is the rule: **one place to
withdraw it from.**

> **`S30` normalises five keys and not `convention_definition`; a forger making the copy coherent
> rewrites that sentence, and the identical-payload signal then inverts to `0 of N`, which is what a
> genuine merge produces. `S30` raises the cost of a false merge to a fabricated input file plus one
> rewritten sentence, and past that there is no in-file signal at all — THE DIFF REMAINS THE CONTROL
> past that rung.**

## 5. Reported and not closed

**Who publishes `waterfall` / `liveness_exclusions` / `retained_by_air_period` at non-adopted grid
arms** · **`awaiting_owner_step` is now exercised by no placeholder** · ***the six blocks' ONE-ARM form
is illustrated nowhere*** — which would need a **fourth** file against a list `0110` fixed at three, **so
the arm reported it rather than adding one.**

**Zero API calls. Step 9 not begun.**
